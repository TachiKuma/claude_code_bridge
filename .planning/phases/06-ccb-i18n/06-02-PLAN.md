---
phase: 06-ccb-i18n
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/i18n/ccb/en.json
  - lib/i18n/ccb/zh.json
  - lib/i18n/ccb/xx.json
  - lib/codex_comm.py
  - lib/gemini_comm.py
  - lib/opencode_comm.py
  - bin/ask
  - bin/cask
  - bin/gask
  - bin/oask
  - bin/dask
  - tests/test_cli_i18n_smoke.py
autonomous: false
requirements: [I18N-02]
user_setup: []
must_haves:
  truths:
    - "CLI 核心用户可见消息优先通过 t() 输出"
    - "协议字符串、命令名和环境变量不进入翻译文件"
    - "en/zh/xx 三份翻译文件保持 key 集同步"
---

<objective>
扩展 CLI 核心翻译覆盖，把当前原型 i18n 从少量调用点扩大到主入口和 provider wrapper。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ccb-i18n/06-CONTEXT.md
@.planning/phases/06-ccb-i18n/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展翻译 key 并迁移 CLI 核心用户文案</name>
  <files>lib/i18n/ccb/en.json, lib/i18n/ccb/zh.json, lib/i18n/ccb/xx.json, lib/codex_comm.py, lib/gemini_comm.py, lib/opencode_comm.py, bin/ask, bin/cask, bin/gask, bin/oask, bin/dask, tests/test_cli_i18n_smoke.py</files>
  <read_first>
    - lib/codex_comm.py
    - lib/gemini_comm.py
    - lib/opencode_comm.py
    - bin/ask
    - bin/cask
    - bin/gask
    - lib/i18n/ccb/en.json
    - scripts/check_protocol_strings.py
  </read_first>
  <action>
1. 先对目标文件做一次“用户可见文本 vs 协议字符串”清洗，只迁移 `print()`、`sys.stderr`、帮助信息、错误提示、状态提示等真正面向用户的文本；保留命令名、provider key、环境变量名、JSON key 不翻译。
2. 在 `lib/i18n/ccb/{en,zh,xx}.json` 中为 CLI 核心补齐新 key，建议最少覆盖以下命名空间：
   - `ccb.ask.*`
   - `ccb.provider.*`
   - `ccb.daemon.*`
   - `ccb.command.*`
   - `ccb.error.*`
3. 将 `bin/ask`、`bin/cask`、`bin/gask`、`bin/oask`、`bin/dask` 以及 `lib/*_comm.py` 中的用户文案改为 `t(...)` 调用；保持原有退出码、协议 guardrail 和 async marker 文本不变。
4. 新增 `tests/test_cli_i18n_smoke.py`，至少覆盖：
   - 默认英文输出；
   - `CCB_LANG=zh` 时的中文 smoke；
   - `CCB_LANG=xx` 时的伪本地化 marker；
   - 关键入口在缺失 key 时不会抛异常。
  </action>
  <verify>
    <automated>
python -m pytest tests/test_cli_i18n_smoke.py -q
python scripts/check_protocol_strings.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `bin/ask`、`bin/cask`、`bin/gask` 至少包含 1 个以上新的 `t(` 调用
    - `tests/test_cli_i18n_smoke.py` 存在并可执行
    - `python -m pytest tests/test_cli_i18n_smoke.py -q` 退出码为 0
    - `python scripts/check_protocol_strings.py` 继续可执行
    - `lib/i18n/ccb/en.json`、`zh.json`、`xx.json` 都包含新增 CLI key
  </acceptance_criteria>
</task>

</tasks>
