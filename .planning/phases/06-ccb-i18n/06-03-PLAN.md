---
phase: 06-ccb-i18n
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - ccb
  - lib/ccb_config.py
  - lib/i18n.py
  - lib/i18n_core.py
  - scripts/check_translation_coverage.py
  - scripts/check_translation_completeness.py
  - .github/workflows/test.yml
  - .pre-commit-config.yaml
  - tests/test_i18n_config.py
autonomous: false
requirements: [I18N-03, I18N-04]
user_setup: []
must_haves:
  truths:
    - "`CCB_LANG`、`ccb config lang` 和 `--lang` 共用同一套语言选择规则"
    - "CI 同时检查协议白名单、翻译覆盖率和翻译完整性"
    - "测试入口对齐 `test/` 与 `tests/` 的现实目录结构"
---

<objective>
建立语言切换和 CI 守卫，让 CLI i18n 具备持续演进能力，而不是一次性迁移。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ccb-i18n/06-CONTEXT.md
@.planning/phases/06-ccb-i18n/06-RESEARCH.md
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一语言切换和翻译守卫</name>
  <files>ccb, lib/ccb_config.py, lib/i18n.py, lib/i18n_core.py, scripts/check_translation_coverage.py, scripts/check_translation_completeness.py, .github/workflows/test.yml, .pre-commit-config.yaml, tests/test_i18n_config.py</files>
  <read_first>
    - ccb
    - lib/ccb_config.py
    - lib/i18n.py
    - .github/workflows/test.yml
    - scripts/check_protocol_strings.py
  </read_first>
  <action>
1. 在 `lib/ccb_config.py` 中扩展 `.ccb-config.json` 读取与写入逻辑，增加固定键 `Language`，允许值 `auto|en|zh|xx`。
2. 在 `ccb` 中新增：
   - 全局 `--lang` 参数；
   - `ccb config lang <auto|en|zh|xx>` 子命令。
   执行规则固定为：`--lang` -> `CCB_LANG` -> `.ccb-config.json: Language` -> locale。
3. 在 `lib/i18n.py` / `lib/i18n_core.py` 中对齐上述优先级，不允许再出现多套互相冲突的语言检测逻辑。
4. 新增 `scripts/check_translation_coverage.py`，扫描 Phase 6 锁定的 CLI 核心文件并报告缺失的 `t()` 调用或缺失的翻译 key。
5. 新增 `scripts/check_translation_completeness.py`，比较 `lib/i18n/ccb/en.json`、`zh.json`、`xx.json` 的 key 集是否一致。
6. 更新 `.github/workflows/test.yml` 和 `.pre-commit-config.yaml`，让以下 3 个检查成为固定门禁：
   - `python scripts/check_protocol_strings.py`
   - `python scripts/check_translation_coverage.py`
   - `python scripts/check_translation_completeness.py`
7. 新增 `tests/test_i18n_config.py`，覆盖 config 文件语言设置、`--lang` 覆盖、`CCB_LANG` 优先级。
  </action>
  <verify>
    <automated>
python scripts/check_protocol_strings.py
python scripts/check_translation_coverage.py
python scripts/check_translation_completeness.py
python -m pytest tests/test_i18n_config.py -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `ccb` 文件中包含 `config lang` 或等价子命令
    - `ccb` 文件中包含 `--lang`
    - `lib/ccb_config.py` 包含 `Language` 配置键处理
    - `scripts/check_translation_coverage.py` 存在
    - `scripts/check_translation_completeness.py` 存在
    - `.github/workflows/test.yml` 调用了上述 3 个检查脚本
    - `python -m pytest tests/test_i18n_config.py -q` 退出码为 0
  </acceptance_criteria>
</task>

</tasks>
