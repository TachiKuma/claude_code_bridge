---
phase: 06-ccb-i18n
plan: 06
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - claude_skills/ask/SKILL.md
  - claude_skills/cping/SKILL.md
  - claude_skills/mounted/SKILL.md
  - claude_skills/pend/SKILL.md
  - claude_skills/review/SKILL.md
  - claude_skills/tp/SKILL.md
  - claude_skills/tr/SKILL.md
  - claude_skills/autonew/SKILL.md
  - claude_skills/continue/SKILL.md
  - claude_skills/file-op/SKILL.md
  - claude_skills/all-plan/SKILL.md
  - codex_skills/ask/SKILL.md
  - codex_skills/file-op/SKILL.md
  - codex_skills/mounted/SKILL.md
  - codex_skills/pend/SKILL.md
  - codex_skills/ping/SKILL.md
  - codex_skills/all-plan/SKILL.md
  - droid_skills/ask/SKILL.md
  - droid_skills/mounted/SKILL.md
  - droid_skills/pend/SKILL.md
  - droid_skills/ping/SKILL.md
  - droid_skills/all-plan/SKILL.md
  - install.ps1
  - install.sh
  - ccb
  - config/claude-md-ccb.md
  - config/claude-md-ccb-route.md
  - config/agents-md-ccb.md
  - config/clinerules-ccb.md
  - config/tmux-ccb.conf
autonomous: false
requirements: [I18N-02, I18N-06]
user_setup: []
must_haves:
  truths:
    - "所有 CCB skill 模板都有中英双语版本，安装时按语言设置选择注入"
    - "install.ps1 的所有 Write-Host/Write-Warning 字符串通过 Get-Msg 字典输出，Write-Warning 统一改为 Write-Host '[WARNING] ...'"
    - "install.sh 的所有 echo/用户文案通过 msg() 函数输出"
    - "ccb 主入口的 argparse description= 和 help= 全部使用 t() 调用"
    - "Config 模板的用户可见说明已翻译，代码块和命令示例保持英文"
  artifacts:
    - path: "claude_skills/*/SKILL.zh.md"
      provides: "Claude skill 中文版本模板"
    - path: "codex_skills/*/SKILL.zh.md"
      provides: "Codex skill 中文版本模板"
    - path: "droid_skills/*/SKILL.zh.md"
      provides: "Droid skill 中文版本模板"
    - path: "install.ps1"
      provides: "扩展后的 Get-Msg 消息字典，覆盖所有用户可见字符串"
    - path: "install.sh"
      provides: "扩展后的 msg() 消息字典，覆盖所有用户可见字符串"
    - path: "ccb"
      provides: "argparse help/description 全部 t() 包裹"
    - path: "config/claude-md-ccb.md"
      provides: "混合翻译的 CLAUDE.md 注入模板"
    - path: "config/agents-md-ccb.md"
      provides: "混合翻译的 AGENTS.md 注入模板"
    - path: "config/clinerules-ccb.md"
      provides: "混合翻译的 .clinerules 注入模板"
    - path: "config/tmux-ccb.conf"
      provides: "混合翻译的 tmux/WezTerm 配置模板"
  key_links:
    - from: "install.ps1 / Install-ClaudeSkills"
      to: "claude_skills/*/SKILL.zh.md"
      via: "语言判断后选择 SKILL.zh.md 或 SKILL.md.bash"
      pattern: "Get-CCBLang.*SKILL"
    - from: "install.sh / install_claude_skills"
      to: "claude_skills/*/SKILL.zh.md"
      via: "语言判断后选择 SKILL.zh.md 或 SKILL.md.bash"
      pattern: "detect_lang.*SKILL"
    - from: "ccb"
      to: "lib/i18n.py::t()"
      via: "argparse description/help 参数使用 t() 调用"
      pattern: "description=.*t\(|help=.*t\("
---

<objective>
完成 CCB i18n 扩展范围的四大增量迁移：Skill 模板双语化、Install 脚本全量迁移、argparse help t() 包裹、Config 模板混合翻译。

Purpose: 将计划 02/03 建立的 CLI 核心翻译能力扩展到用户接触面最广的模板和脚本层，确保 CCB 安装后的首个用户体验（help 输出、安装过程、skill 提示、配置模板）完全本地化。
Output: 双语 skill 模板、扩展后的安装脚本消息字典、t() 包裹的 argparse help、混合翻译的 config 模板。
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ccb-i18n/06-CONTEXT.md
@.planning/phases/06-ccb-i18n/06-RESEARCH.md

<!-- Key interfaces from plans 02/03 that this plan builds upon -->
<interfaces>
From lib/i18n.py (after plan 02):
- `t(key: str, **kwargs) -> str` - unified translation function
- Language priority: `--lang` > `CCB_LANG` > `.ccb-config.json: Language` > locale

From ccb (after plan 03):
- `--lang` CLI flag for per-invocation language override
- `ccb config lang` subcommand for persistent language setting

From install.ps1:
- `Get-CCBLang()` returns "en" or "zh" based on `CCB_LANG` or system locale
- `Get-Msg($Key)` looks up `$msgs[$Key][$script:CCBLang]` with 7 existing keys
- `Install-ClaudeSkills` copies from `claude_skills/*/SKILL.md.bash` or `SKILL.md` to `~/.claude/skills/*/SKILL.md`

From install.sh:
- `detect_lang()` returns "en" or "zh"
- `msg()` function for message lookup with existing dictionary
- `install_claude_skills()` copies from `claude_skills/*/` to `~/.claude/skills/*/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill 模板双语化 + Install 脚本全量消息迁移</name>
  <files>claude_skills/ask/SKILL.md, claude_skills/cping/SKILL.md, claude_skills/mounted/SKILL.md, claude_skills/pend/SKILL.md, claude_skills/review/SKILL.md, claude_skills/tp/SKILL.md, claude_skills/tr/SKILL.md, claude_skills/autonew/SKILL.md, claude_skills/continue/SKILL.md, claude_skills/file-op/SKILL.md, claude_skills/all-plan/SKILL.md, codex_skills/ask/SKILL.md, codex_skills/file-op/SKILL.md, codex_skills/mounted/SKILL.md, codex_skills/pend/SKILL.md, codex_skills/ping/SKILL.md, codex_skills/all-plan/SKILL.md, droid_skills/ask/SKILL.md, droid_skills/mounted/SKILL.md, droid_skills/pend/SKILL.md, droid_skills/ping/SKILL.md, droid_skills/all-plan/SKILL.md, install.ps1, install.sh</files>
  <read_first>
    - claude_skills/ask/SKILL.md
    - claude_skills/cping/SKILL.md
    - claude_skills/pend/SKILL.md
    - claude_skills/review/SKILL.md
    - claude_skills/all-plan/SKILL.md
    - codex_skills/ask/SKILL.md
    - codex_skills/ping/SKILL.md
    - droid_skills/ask/SKILL.md
    - install.ps1
    - install.sh
  </read_first>
  <action>
**Part A: Skill 模板双语化 (per D-09)**

1. 为每个 CCB skill 创建中文翻译版本。命名约定：
   - `claude_skills/<name>/SKILL.zh.md` — 中文版（用于 Claude skills，当前有 11 个）
   - `codex_skills/<name>/SKILL.zh.md` — 中文版（用于 Codex skills，当前有 6 个）
   - `droid_skills/<name>/SKILL.zh.md` — 中文版（用于 Droid skills，当前有 5 个，含 SKILL.md 和 ask.md）
   现有的 `SKILL.md`（或 `SKILL.md.bash`）保留为英文默认版本。

2. 翻译范围包括每个 SKILL.md 的：
   - YAML frontmatter: `description` 和 `metadata.short-description`
   - `#` 标题
   - `##` 小节标题和描述文本
   - 使用说明、规则指令、注意事项等面向用户的段落
   **不翻译**：代码块内容、命令示例、环境变量名、API 路径、provider 名称、completion hook 标记符。

3. 翻译时保持 AI prompt 的可执行性——翻译后的指令文本必须能让 AI 正确理解和执行。例如：
   - "Send the user's request to specified AI provider asynchronously" -> "将用户请求异步发送给指定的 AI provider"
   - provider 名 (gemini/codex/opencode/droid) 保持英文不翻译
   - 命令格式模板（如 `Bash(CCB_CALLER=claude ask $PROVIDER "$MESSAGE")`）保持英文

4. 修改安装脚本的 skill 安装函数，根据语言选择注入对应版本：
   - **install.ps1**: 在 `Install-ClaudeSkills` 函数中（当前位于约第 351 行调用），修改文件选择逻辑为：当 `$script:CCBLang -eq "zh"` 时优先选择 `SKILL.zh.md`，否则选择 `SKILL.md.bash`（如果存在）或 `SKILL.md`。对 `Install-CodexSkills`（约第 428 行）和 `Install-DroidSkills`（约第 475 行）做同样处理。
   - **install.sh**: 在 `install_claude_skills()` 函数（当前位于约第 700 行）中，修改文件选择逻辑：当 `detect_lang` 返回 `zh` 时优先选择 `SKILL.zh.md`，否则选择 `SKILL.md.bash` 或 `SKILL.md`。

**Part B: install.ps1 消息全量迁移 (per D-10)**

5. 扩展 `install.ps1` 中 `Get-Msg()` 函数的消息字典。当前字典仅有 7 个 key，需要扫描全部 ~197 个 Write-Host/Write-Warning/Write-Error 调用，将所有用户可见字符串迁移到消息字典。

   消息字典的 key 命名规范使用 `install.<section>.<purpose>` 格式，例如：
   - `"install.usage.line"` = "Usage line text"
   - `"install.python.found"` = "Found Python at {path}"
   - `"install.link.creating"` = "Creating symlink: {src} -> {dst}"
   - `"install.skill.installing"` = "Installing Claude skills..."
   - `"install.skill.updated"` = "Updated skill: {name}"
   - `"install.warning.windows"` = "[WARNING] You are installing ccb in native Windows environment"
   - `"install.error.python_old"` = "[ERROR] Python version too old: {version}"

6. 将所有 `Write-Warning "..."` 调用替换为 `Write-Host "[WARNING] ..."` 格式（per D-10），消除 PowerShell 系统前缀的本地化不一致问题。

7. 将所有硬编码 `Write-Host` 中的用户文案替换为 `Get-Msg` 调用，格式为 `$(Get-Msg "install.xxx.yyy") -f $Arg1`。注意：协议字符串（路径、命令名、环境变量名）保持英文不翻译，通过 `$Arg1` 参数注入。

**Part C: install.sh 消息全量迁移 (per D-10)**

8. 扩展 `install.sh` 中 `msg()` 函数的消息字典。扫描全部 ~99 个 echo/用户文案调用，将遗漏的硬编码字符串补齐到消息字典。

   消息字典 key 命名规范与 install.ps1 保持一致（`install.<section>.<purpose>`），确保两个脚本的消息 key 语义对齐。

9. 将所有硬编码 `echo` 中的用户文案替换为 `msg "install.xxx.yyy"` 调用。协议字符串保持英文，通过格式化参数注入。
  </action>
  <verify>
    <automated>
# Verify zh skill files exist for all CCB skills
find claude_skills -name "SKILL.zh.md" | wc -l
find codex_skills -name "SKILL.zh.md" | wc -l
find droid_skills -name "SKILL.zh.md" | wc -l

# Verify install.ps1 Get-Msg dictionary has expanded (should have 50+ keys)
grep -c '"install\.' install.ps1

# Verify no Write-Warning remains in install.ps1
grep -c 'Write-Warning' install.ps1 || echo "0 (good)"

# Verify install.sh msg dictionary has expanded
grep -c '"install\.' install.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - `claude_skills/` 下每个 skill 目录都包含 `SKILL.zh.md` 文件（至少 11 个）
    - `codex_skills/` 下每个 skill 目录都包含 `SKILL.zh.md` 文件（至少 6 个）
    - `droid_skills/` 下每个 skill 目录都包含 `SKILL.zh.md` 文件（至少 5 个）
    - 每个 `SKILL.zh.md` 的 frontmatter 中 `description` 和 `metadata.short-description` 是中文
    - `install.ps1` 中 `Get-Msg` 的 `$msgs` 哈希表包含 50 个以上 key（当前仅 7 个）
    - `install.ps1` 中 `grep -c 'Write-Warning'` 输出为 0（全部替换为 Write-Host）
    - `install.ps1` 的 `Install-ClaudeSkills` 函数中包含 `SKILL.zh.md` 选择逻辑
    - `install.sh` 的 `install_claude_skills()` 函数中包含 `SKILL.zh.md` 选择逻辑
    - `install.sh` 中 `msg` 函数的消息字典包含 `install.` 前缀的 key（50 个以上）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: argparse help 全量 t() 包裹 + Config 模板混合翻译</name>
  <files>ccb, config/claude-md-ccb.md, config/claude-md-ccb-route.md, config/agents-md-ccb.md, config/clinerules-ccb.md, config/tmux-ccb.conf</files>
  <read_first>
    - ccb
    - config/claude-md-ccb.md
    - config/claude-md-ccb-route.md
    - config/agents-md-ccb.md
    - config/clinerules-ccb.md
    - config/tmux-ccb.conf
    - lib/i18n/ccb/en.json
    - lib/i18n/ccb/zh.json
    - lib/i18n/ccb/xx.json
  </read_first>
  <action>
**Part A: argparse help 全量 t() 包裹 (per D-11)**

1. 在 `ccb` 主入口文件中，将所有 argparse 的 `description=` 和 `help=` 参数替换为 `t()` 调用。当前已知有 ~34 处需要替换，分布在以下 parser 定义中：
   - 主 parser: `argparse.ArgumentParser(description="Claude AI unified launcher")` -> `description=t("ccb.main.description")`
   - `mail` 子命令: `description="Mail service commands"` -> `description=t("ccb.mail.description")`
   - `droid` 子命令: `description="Droid-specific commands"` -> `description=t("ccb.droid.description")`
   - `config` 子命令: `description="CCB project configuration"` -> `description=t("ccb.config.description")`
   - `start` parser: `description="Claude AI unified launcher"` -> `description=t("ccb.start.description")`
   - 所有 `add_argument(..., help="...")` 中的 help 文本

2. 由于 plan 03 已建立 `--lang` 参数和语言优先级链，需要确保 parser 构建发生在 `t()` 可用之后。具体做法：
   - `from i18n import t` 已经在文件头部（第 47 行）
   - argparse parser 定义在函数内部（`build_start_parser()` 约第 4932 行，`build_main_parser()` 约第 4896 行）
   - 确保在调用这些函数之前，`i18n` 模块已经完成语言检测初始化（这已在 plan 01/03 中解决）
   - 如果 `t()` 在 parser 构建时尚未初始化，使用 lambda 延迟求值：`description=lambda: t("ccb.main.description")`（仅在必要时采用此方案）

3. 在 `lib/i18n/ccb/{en,zh,xx}.json` 中添加 `ccb.main.*`、`ccb.mail.*`、`ccb.droid.*`、`ccb.config.*`、`ccb.start.*`、`ccb.kill.*`、`ccb.update.*` 等命名空间下的翻译 key。

**Part B: Config 模板混合翻译 (per D-12)**

4. 为每个 config 模板创建中文翻译版本：
   - `config/claude-md-ccb.md` -> `config/claude-md-ccb.zh.md`
   - `config/claude-md-ccb-route.md` -> `config/claude-md-ccb-route.zh.md`
   - `config/agents-md-ccb.md` -> `config/agents-md-ccb.zh.md`
   - `config/clinerules-ccb.md` -> `config/clinerules-ccb.zh.md`
   - `config/tmux-ccb.conf` -> `config/tmux-ccb.zh.conf`

   混合翻译策略（严格按 D-12）：
   - **翻译**：注释行（以 `#` 开头）、角色描述表格中的 Description 列、规则说明段落、章节标题
   - **不翻译**：代码块（`` ``` `` 围栏内容）、JSON schema、命令示例（`Bash(...)` 等）、环境变量名、HTML/XML 标记（如 `<!-- CCB_CONFIG_START -->`）、provider 名称、表格中的 Role/Provider 列
   - **保持一致**：所有 CCB marker 注释（如 `<!-- CCB_ROLES_START/END -->`、`<!-- CCB_CONFIG_START/END -->`）保持英文原样

5. 修改 `install.ps1` 和 `install.sh` 中的 config 模板注入逻辑：
   - 在写入 `CLAUDE.md`、`AGENTS.md`、`.clinerules`、`tmux.conf` 时，根据语言选择注入 `.zh.md`/`.zh.conf` 版本还是英文原版
   - `install.ps1` 中 config 注入相关函数（搜索 `claude-md-ccb`、`agents-md-ccb`、`clinerules-ccb`、`tmux-ccb`）
   - `install.sh` 中 config 注入相关函数（同样搜索上述文件名）

   选择逻辑：
   ```powershell
   # install.ps1
   $templateFile = if ($script:CCBLang -eq "zh") { "claude-md-ccb.zh.md" } else { "claude-md-ccb.md" }
   ```
   ```bash
   # install.sh
   template_file="claude-md-ccb${lang_suffix}.md"
   ```
  </action>
  <verify>
    <automated>
# Verify argparse uses t() calls instead of hardcoded strings in ccb
grep -c 'description=t(' ccb
grep -c 'help=t(' ccb

# Verify no hardcoded description= remains (excluding t() calls)
grep -c 'description="[^"]' ccb || echo "0 (good)"

# Verify zh config templates exist
ls config/claude-md-ccb.zh.md config/agents-md-ccb.zh.md config/clinerules-ccb.zh.md config/tmux-ccb.zh.conf

# Verify translation files have new argparse keys
grep -c 'ccb.main\.' lib/i18n/ccb/en.json
grep -c 'ccb.main\.' lib/i18n/ccb/zh.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `ccb` 文件中 `grep -c 'description=t('` 输出 >= 5（覆盖所有 parser 的 description）
    - `ccb` 文件中 `grep -c 'help=t('` 输出 >= 20（覆盖主要 help 文本）
    - `ccb` 文件中不存在 `description="Claude AI unified launcher"` 或其他原始硬编码 description 字符串
    - `config/claude-md-ccb.zh.md` 存在，且 CCB marker 注释（`<!-- CCB_*_START -->`）保持英文
    - `config/agents-md-ccb.zh.md` 存在，且角色名称表格中 Role 和 Provider 列保持英文
    - `config/clinerules-ccb.zh.md` 存在
    - `config/tmux-ccb.zh.conf` 存在，且 `set -g` 等配置指令保持英文
    - `lib/i18n/ccb/en.json` 和 `zh.json` 都包含 `ccb.main`、`ccb.mail`、`ccb.config`、`ccb.start` 等 argparse 翻译 key
    - `install.ps1` 和 `install.sh` 的 config 模板注入逻辑支持按语言选择 `.zh.` 版本
  </acceptance_criteria>
</task>

</tasks>

<verification>
整体验证步骤：

1. **Skill 双语覆盖**：
   - `find claude_skills codex_skills droid_skills -name "SKILL.zh.md" | wc -l` >= 22（覆盖所有 skill）
   - 每个 `SKILL.zh.md` 的 frontmatter description 为中文

2. **Install 脚本迁移**：
   - `install.ps1` 中 `Write-Warning` 调用数为 0
   - `install.ps1` 的 `Get-Msg` 字典 key 数 >= 50
   - `install.sh` 的 `msg()` 字典包含 `install.` 前缀 key >= 50

3. **Argparse t() 包裹**：
   - `grep -c 'description=t(' ccb` >= 5
   - `grep -c 'help=t(' ccb` >= 20
   - 翻译文件包含对应的 argparse key

4. **Config 模板混合翻译**：
   - 4 个 `.zh.` 配置模板文件存在
   - marker 注释保持英文，用户说明已翻译

5. **回归检查**：
   - `python scripts/check_protocol_strings.py` 退出码为 0
   - `python scripts/check_translation_completeness.py` 退出码为 0
   - `python scripts/check_translation_coverage.py` 退出码为 0
</verification>

<success_criteria>
1. 所有 CCB skill 模板（claude/codex/droid）都有中英双语版本，安装脚本根据语言设置选择注入
2. install.ps1 全量 ~197 个 Write-Host/Write-Warning 字符串迁移到 Get-Msg 字典，Write-Warning 统一为 Write-Host "[WARNING] ..."
3. install.sh 全量 ~99 个 echo/用户文案迁移到 msg() 字典
4. ccb argparse 的所有 description= 和 help= 使用 t() 调用，help 输出与用户语言设置一致
5. Config 模板采用混合翻译策略：用户可见说明已翻译，代码块/命令/JSON schema 保持英文
6. 所有变更通过协议白名单检查、翻译覆盖率和翻译完整性检查
</success_criteria>

<output>
After completion, create `.planning/phases/06-ccb-i18n/06-06-SUMMARY.md`
</output>
