---
phase: 01-代码库分析
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-代码库分析/analysis-tools/scan_ccb.py
  - .planning/phases/01-代码库分析/results/ccb_strings.json
autonomous: true
requirements:
  - ANALYSIS-01

must_haves:
  truths:
    - "CCB 代码库中所有硬编码字符串已被提取"
    - "每个字符串记录包含文件路径、行号、内容"
    - "扫描结果以 JSON 格式存储"
  artifacts:
    - path: ".planning/phases/01-代码库分析/analysis-tools/scan_ccb.py"
      provides: "Python AST 扫描器"
      min_lines: 50
    - path: ".planning/phases/01-代码库分析/results/ccb_strings.json"
      provides: "CCB 提取的所有字符串"
      contains: "file"
  key_links:
    - from: "scan_ccb.py"
      to: "lib/*.py"
      via: "ast.parse() 遍历"
      pattern: "ast\\.parse"
    - from: "scan_ccb.py"
      to: "ccb_strings.json"
      via: "json.dump()"
      pattern: "json\\.dump"
---

<objective>
扫描 CCB 代码库（98 个 Python 文件），使用 AST 静态分析提取所有硬编码字符串。

Purpose: 为 i18n 改造提供完整的文本清单，确保不遗漏任何需要翻译的内容
Output: ccb_strings.json（包含所有字符串的位置和内容）
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/PROJECT.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/STATE.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/phases/01-代码库分析/01-CONTEXT.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/phases/01-代码库分析/01-RESEARCH.md
</context>

<interfaces>
<!-- 现有 i18n.py 的关键接口 -->
From lib/i18n.py:
```python
MESSAGES = {
    "en": { "key": "value" },
    "zh": { "key": "值" }
}

def t(key: str, **kwargs) -> str:
    """Get translated message by key"""
    pass
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 CCB Python 扫描器</name>
  <files>.planning/phases/01-代码库分析/analysis-tools/scan_ccb.py</files>
  <read_first>
    - lib/i18n.py（了解现有字符串格式）
    - .planning/phases/01-代码库分析/01-RESEARCH.md（AST 遍历示例代码）
  </read_first>
  <action>
创建 Python AST 扫描器，遍历 lib/ 目录下所有 .py 文件，提取字符串字面量。

实现要点（per D-01, D-02）:
- 使用 ast.NodeVisitor 类
- 实现 visit_Constant() 方法处理 Python 3.8+ 字符串节点
- 记录每个字符串的: filepath（相对路径）, line（行号）, col（列偏移）, value（字符串内容）
- 跳过空字符串和单字符字符串
- 使用 pathlib.Path.rglob('*.py') 遍历 lib/ 目录
- 错误处理: 文件读取失败时打印警告并继续
- 输出 JSON 格式: [{"file": "lib/i18n.py", "line": 18, "col": 8, "value": "no_terminal_backend"}, ...]

不要实现分类逻辑（留给 Plan 02），仅提取所有字符串。
  </action>
  <verify>
    <automated>python .planning/phases/01-代码库分析/analysis-tools/scan_ccb.py</automated>
  </verify>
  <acceptance_criteria>
    - scan_ccb.py 包含 class StringExtractor(ast.NodeVisitor)
    - scan_ccb.py 包含 def visit_Constant(self, node)
    - scan_ccb.py 包含 Path('lib').rglob('*.py')
    - 执行脚本无语法错误
  </acceptance_criteria>
  <done>扫描器脚本创建完成，能够遍历 lib/ 目录并提取字符串</done>
</task>

<task type="auto">
  <name>Task 2: 执行扫描并生成结果</name>
  <files>.planning/phases/01-代码库分析/results/ccb_strings.json</files>
  <read_first>
    - .planning/phases/01-代码库分析/analysis-tools/scan_ccb.py（确认扫描器实现）
  </read_first>
  <action>
运行 CCB 扫描器，生成完整的字符串清单。

执行步骤:
1. 创建 results/ 目录（如不存在）
2. 运行: python .planning/phases/01-代码库分析/analysis-tools/scan_ccb.py > .planning/phases/01-代码库分析/results/ccb_strings.json
3. 验证输出文件存在且为有效 JSON
4. 统计提取的字符串数量（预期 500-1500 条，基于 98 个文件）

输出格式验证:
- JSON 数组格式
- 每个条目包含 file, line, col, value 字段
- file 路径为相对路径（lib/xxx.py）
  </action>
  <verify>
    <automated>test -f .planning/phases/01-代码库分析/results/ccb_strings.json && python -c "import json; data=json.load(open('.planning/phases/01-代码库分析/results/ccb_strings.json')); print(f'{len(data)} strings extracted'); assert len(data) > 100"</automated>
  </verify>
  <acceptance_criteria>
    - ccb_strings.json 文件存在
    - 文件为有效 JSON 数组
    - 数组包含 > 100 个条目
    - 每个条目包含 file, line, value 字段
    - grep '"file": "lib/' .planning/phases/01-代码库分析/results/ccb_strings.json 返回多个匹配
  </acceptance_criteria>
  <done>CCB 代码库扫描完成，所有硬编码字符串已提取到 JSON 文件</done>
</task>

</tasks>

<verification>
- [ ] scan_ccb.py 脚本存在且可执行
- [ ] ccb_strings.json 包含 lib/ 目录下所有 Python 文件的字符串
- [ ] JSON 格式正确，包含文件路径、行号、字符串内容
- [ ] 提取的字符串数量合理（> 100 条）
</verification>

<success_criteria>
- CCB 代码库（98 个 Python 文件）已完整扫描
- 所有字符串字面量已提取并记录位置信息
- 结果以结构化 JSON 格式存储，可供后续分类使用
</success_criteria>

<output>
After completion, create `.planning/phases/01-代码库分析/01-01-SUMMARY.md`
</output>
