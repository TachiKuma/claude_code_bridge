---
phase: 01-代码库分析
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-代码库分析/analysis-tools/scan_gsd.js
  - .planning/phases/01-代码库分析/analysis-tools/classify.py
  - .planning/phases/01-代码库分析/results/gsd_strings.json
  - .planning/phases/01-代码库分析/results/classified.json
autonomous: true
requirements:
  - ANALYSIS-02
  - ANALYSIS-03

must_haves:
  truths:
    - "GSD 代码库中所有硬编码字符串已被提取"
    - "协议字符串和人类文本已被正确分类"
    - "分类规则基于命名模式（全大写、特定前缀/后缀）"
  artifacts:
    - path: ".planning/phases/01-代码库分析/analysis-tools/scan_gsd.js"
      provides: "JavaScript AST 扫描器"
      min_lines: 40
    - path: ".planning/phases/01-代码库分析/analysis-tools/classify.py"
      provides: "字符串分类器"
      min_lines: 30
    - path: ".planning/phases/01-代码库分析/results/gsd_strings.json"
      provides: "GSD 提取的所有字符串"
      contains: "file"
    - path: ".planning/phases/01-代码库分析/results/classified.json"
      provides: "分类后的结果"
      contains: "protocol"
  key_links:
    - from: "scan_gsd.js"
      to: ".claude/get-shit-done/**/*.js"
      via: "@babel/parser"
      pattern: "parser\\.parse"
    - from: "classify.py"
      to: "gsd_strings.json + ccb_strings.json"
      via: "正则匹配"
      pattern: "re\\.match"
---

<objective>
扫描 GSD 代码库（18 个 JavaScript 文件），提取所有硬编码字符串，并实现分类器区分协议字符串和人类文本。

Purpose: 完成 GSD 文本提取，建立自动分类机制，为后续 i18n 改造提供准确的文本分类
Output: gsd_strings.json（GSD 字符串清单）+ classified.json（分类结果）
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/PROJECT.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/ROADMAP.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/STATE.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/phases/01-代码库分析/01-CONTEXT.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.planning/phases/01-代码库分析/01-RESEARCH.md
</context>

<interfaces>
<!-- 协议字符串特征（from CONTEXT.md D-05） -->
协议字符串识别规则:
- 全大写: CCB_DONE, ASK_RESPONSE
- CCB_ 前缀: CCB_LANG, CCB_TERMINAL
- GSD_ 前缀: GSD_MODE
- _DONE 后缀: TASK_DONE
- RPC 协议: ask.response, cask.reply
- 文件扩展名: .json, .md, .py
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 GSD JavaScript 扫描器</name>
  <files>.planning/phases/01-代码库分析/analysis-tools/scan_gsd.js</files>
  <read_first>
    - .planning/phases/01-代码库分析/01-RESEARCH.md（Babel 使用示例）
    - .claude/get-shit-done/bin/gsd-tools.cjs（了解 GSD 代码风格）
  </read_first>
  <action>
创建 JavaScript AST 扫描器，遍历 .claude/get-shit-done/ 目录下所有 .js 和 .cjs 文件。

实现要点（per D-01, D-02）:
- 使用 @babel/parser 和 @babel/traverse
- 处理 StringLiteral 节点（普通字符串）
- 处理 TemplateLiteral 节点（模板字符串的静态部分）
- 记录: file（相对路径）, line, col, value, type（'string' 或 'template'）
- 使用 Node.js fs.readdirSync 递归遍历目录
- 跳过 node_modules 目录
- 输出 JSON 到 stdout

脚本结构:
```javascript
const parser = require('@babel/parser');
const traverse = require('@babel/traverse').default;
const fs = require('fs');
const path = require('path');

// 递归扫描目录
// 解析每个 .js/.cjs 文件
// 提取 StringLiteral 和 TemplateLiteral
// 输出 JSON 数组
```
  </action>
  <verify>
    <automated>node .planning/phases/01-代码库分析/analysis-tools/scan_gsd.js > /dev/null 2>&1 && echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - scan_gsd.js 包含 require('@babel/parser')
    - scan_gsd.js 包含 require('@babel/traverse')
    - scan_gsd.js 包含 StringLiteral(path)
    - scan_gsd.js 包含 TemplateLiteral(path)
    - 执行脚本无语法错误
  </acceptance_criteria>
  <done>GSD 扫描器创建完成，能够提取 JavaScript 字符串字面量</done>
</task>

<task type="auto">
  <name>Task 2: 创建字符串分类器</name>
  <files>.planning/phases/01-代码库分析/analysis-tools/classify.py</files>
  <read_first>
    - .planning/phases/01-代码库分析/01-CONTEXT.md（D-04, D-05, D-06 分类标准）
    - .planning/phases/01-代码库分析/01-RESEARCH.md（分类器示例代码）
  </read_first>
  <action>
创建分类器，读取 ccb_strings.json 和 gsd_strings.json，区分协议字符串和人类文本。

分类规则（per D-04, D-05, D-06）:
- 协议字符串模式:
  - r'^[A-Z_]{3,}$' （全大写，至少 3 字符）
  - r'^CCB_[A-Z_]+$' （CCB 前缀）
  - r'^GSD_[A-Z_]+$' （GSD 前缀）
  - r'_DONE$' （DONE 后缀）
  - r'^ask\.[a-z_]+$' （RPC 协议）
  - r'^\.[a-z]+$' （文件扩展名）
- 人类文本: 所有其他字符串

输出格式:
```json
{
  "protocol": [
    {"file": "lib/i18n.py", "line": 177, "value": "CCB_LANG", "reason": "CCB_前缀"}
  ],
  "human": [
    {"file": "lib/i18n.py", "line": 18, "value": "No terminal backend detected", "reason": "用户消息"}
  ],
  "stats": {
    "total": 1500,
    "protocol": 94,
    "human": 1406
  }
}
```

读取两个 JSON 文件，合并后分类，输出到 classified.json。
  </action>
  <verify>
    <automated>python .planning/phases/01-代码库分析/analysis-tools/classify.py</automated>
  </verify>
  <acceptance_criteria>
    - classify.py 包含 PROTOCOL_PATTERNS = [
    - classify.py 包含 def classify_string(text: str)
    - classify.py 包含 json.load(open('ccb_strings.json'))
    - classify.py 包含 json.load(open('gsd_strings.json'))
    - 执行脚本无语法错误
  </acceptance_criteria>
  <done>分类器创建完成，能够根据命名模式区分协议和人类文本</done>
</task>

<task type="auto">
  <name>Task 3: 执行 GSD 扫描和分类</name>
  <files>
    .planning/phases/01-代码库分析/results/gsd_strings.json
    .planning/phases/01-代码库分析/results/classified.json
  </files>
  <read_first>
    - .planning/phases/01-代码库分析/analysis-tools/scan_gsd.js
    - .planning/phases/01-代码库分析/analysis-tools/classify.py
    - .planning/phases/01-代码库分析/results/ccb_strings.json（Plan 01 的输出）
  </read_first>
  <action>
执行 GSD 扫描和分类流程。

步骤:
1. 运行 GSD 扫描器: node .planning/phases/01-代码库分析/analysis-tools/scan_gsd.js > .planning/phases/01-代码库分析/results/gsd_strings.json
2. 验证 gsd_strings.json 有效（预期 200-500 条字符串，基于 18 个文件）
3. 运行分类器: cd .planning/phases/01-代码库分析/results && python ../analysis-tools/classify.py
4. 验证 classified.json 包含 protocol 和 human 两个数组
5. 打印统计信息: 总数、协议字符串数、人类文本数

预期结果:
- GSD 字符串数: 200-500
- 协议字符串占比: ~5-10%（基于研究中的 94+ 协议标记）
- 人类文本占比: ~90-95%
  </action>
  <verify>
    <automated>test -f .planning/phases/01-代码库分析/results/gsd_strings.json && test -f .planning/phases/01-代码库分析/results/classified.json && python -c "import json; c=json.load(open('.planning/phases/01-代码库分析/results/classified.json')); print(f\"Protocol: {len(c['protocol'])}, Human: {len(c['human'])}\"); assert 'protocol' in c and 'human' in c"</automated>
  </verify>
  <acceptance_criteria>
    - gsd_strings.json 文件存在且为有效 JSON
    - classified.json 文件存在且包含 protocol 和 human 字段
    - classified.json 包含 stats 字段显示统计信息
    - grep '"protocol"' .planning/phases/01-代码库分析/results/classified.json 返回匹配
    - grep '"human"' .planning/phases/01-代码库分析/results/classified.json 返回匹配
  </acceptance_criteria>
  <done>GSD 扫描完成，所有字符串已分类为协议或人类文本</done>
</task>

</tasks>

<verification>
- [ ] scan_gsd.js 能够提取 JavaScript 字符串（包括模板字符串）
- [ ] classify.py 正确实现协议字符串识别规则
- [ ] gsd_strings.json 包含 GSD 代码库的所有字符串
- [ ] classified.json 包含 CCB + GSD 的完整分类结果
- [ ] 协议字符串数量合理（预期 ~100 条）
</verification>

<success_criteria>
- GSD 代码库（18 个 JavaScript 文件）已完整扫描
- 协议字符串和人类文本已自动分类
- 分类规则基于用户决策的命名模式
- 结果以结构化 JSON 格式存储
</success_criteria>

<output>
After completion, create `.planning/phases/01-代码库分析/01-02-SUMMARY.md`
</output>
