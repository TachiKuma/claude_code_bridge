---
phase: 07-windows-native-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/windows/__init__.py
  - tests/windows/conftest.py
  - tests/windows/test_perf_daemon.py
  - tests/windows/test_perf_socket.py
  - tests/windows/test_e2e_commands.py
autonomous: true
requirements: [WIN-01]
user_setup:
  - service: psutil
    why: "daemon memory measurement (test_perf_daemon.py)"
    env_vars:
      - name: (none needed)
        source: "pip install psutil"
    dashboard_config: []

must_haves:
  truths:
    - "pytest can discover and run tests in tests/windows/ directory"
    - "daemon cold start time is measured and compared against 3s threshold (D-06)"
    - "command response time is measured and compared against 500ms threshold (D-07)"
    - "daemon memory usage is measured and compared against 50MB threshold (D-08)"
    - "socket communication round-trip time is measured"
    - "Core commands (ask, pend, ccb start) work end-to-end on native Windows (Success Criteria #1, D-16)"
    - "Daemon can be stopped and state file is cleaned up after shutdown (D-16 process management)"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest configuration with windows markers"
      contains: "[pytest]"
    - path: "tests/windows/__init__.py"
      provides: "Python package marker"
    - path: "tests/windows/conftest.py"
      provides: "shared fixtures: isolated temp dir, env isolation, daemon lifecycle helpers"
      contains: "@pytest.fixture"
    - path: "tests/windows/test_perf_daemon.py"
      provides: "daemon cold start, command response, memory benchmark tests"
      contains: "def test_daemon_cold_start"
    - path: "tests/windows/test_perf_socket.py"
      provides: "socket communication efficiency tests"
      contains: "def test_socket"
    - path: "tests/windows/test_e2e_commands.py"
      provides: "end-to-end core command functionality tests"
      contains: "def test_e2e"
  key_links:
    - from: "tests/windows/conftest.py"
      to: "lib/askd_server.py"
      via: "imports AskDaemonServer for lifecycle management"
      pattern: "from lib.askd_server import"
    - from: "tests/windows/test_perf_daemon.py"
      to: "tests/windows/conftest.py"
      via: "uses daemon_start fixture"
      pattern: "daemon_start"
    - from: "tests/windows/test_e2e_commands.py"
      to: "bin/ask"
      via: "subprocess call to ask command"
      pattern: "subprocess.*ask"
    - from: "tests/windows/test_e2e_commands.py"
      to: "bin/pend"
      via: "subprocess call to pend command"
      pattern: "subprocess.*pend"
---

<objective>
Create the test infrastructure, performance audit tests, daemon lifecycle management tests, and end-to-end command functionality tests for the Windows native environment.

Purpose: Establish the shared pytest configuration, fixtures, and performance benchmarks that all subsequent audit plans depend on. Measure daemon cold start time (D-06: <3s), command response time (D-07: <500ms), memory usage (D-08: <50MB), and socket round-trip efficiency. Add daemon stop/restart/stale process tests per D-16 process management requirement. Add end-to-end command tests (ask/pend/ccb start) to verify Success Criteria #1.

Output: pytest.ini, shared conftest.py, test_perf_daemon.py, test_perf_socket.py, test_e2e_commands.py
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-windows-native-audit/07-CONTEXT.md
@.planning/phases/07-windows-native-audit/07-RESEARCH.md

<interfaces>
From lib/askd_server.py:
```python
class AskDaemonServer:
    def __init__(self, *, spec, host, port, token, state_file, request_handler,
                 request_queue_size=None, on_stop=None, parent_pid=None,
                 managed=None, work_dir=None): ...
    def serve_forever(self) -> int: ...

from lib.providers import ProviderDaemonSpec  # dataclass with protocol_prefix, daemon_key, lock_name, etc.
```

From lib/askd_runtime.py:
```python
def random_token() -> str: ...  # returns os.urandom(16).hex()
def run_dir() -> Path: ...      # returns ~/.cache/ccb or %LOCALAPPDATA%/ccb on Windows
def state_file_path(name: str) -> Path: ...
def write_log(path: Path, msg: str) -> None: ...
```

From lib/askd/daemon.py:
```python
class UnifiedAskDaemon:
    def __init__(self, host="127.0.0.1", port=0, *, state_file=None, registry=None, work_dir=None): ...
    def serve_forever(self) -> int: ...
    def _cleanup_state_file(self) -> None: ...

def read_state(state_file=None) -> Optional[dict]: ...
def ping_daemon(timeout_s=0.5, state_file=None) -> bool: ...
def shutdown_daemon(timeout_s=1.0, state_file=None) -> bool: ...
```

From lib/providers.py (relevant spec):
```python
# ProviderDaemonSpec is a dataclass with fields:
#   protocol_prefix, daemon_key, lock_name, log_file_name, idle_timeout_env
# Example: UNIFIED_SPEC has protocol_prefix="askd", daemon_key="askd"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test infrastructure (pytest.ini + conftest.py + __init__.py)</name>
  <files>pytest.ini, tests/windows/__init__.py, tests/windows/conftest.py</files>
  <read_first>
    - pytest.ini (does not exist yet — create new)
    - tests/windows/__init__.py (does not exist yet — create new)
    - tests/windows/conftest.py (does not exist yet — create new)
    - tests/test_i18n_core.py (reference for existing test patterns and imports)
    - lib/askd_server.py (understand AskDaemonServer constructor signature)
    - lib/providers.py (understand ProviderDaemonSpec fields)
  </read_first>
  <behavior>
    - Test 1: pytest.ini has [pytest] section with testpaths = tests and markers for windows, perf, compat, security
    - Test 2: conftest.py defines fixture `isolated_env` that saves/restores os.environ around each test
    - Test 3: conftest.py defines fixture `temp_run_dir` that creates a temporary CCB_RUN_DIR and cleans up after
    - Test 4: conftest.py defines fixture `daemon_spec` returning a test ProviderDaemonSpec
    - Test 5: conftest.py defines fixture `daemon_token` returning a fixed test token string
    - Test 6: conftest.py defines fixture `free_port` that finds an available TCP port using socket.bind
  </behavior>
  <action>
    Create `pytest.ini` at project root with:
    ```ini
    [pytest]
    testpaths = tests
    markers =
        windows: Windows-specific tests
        perf: Performance benchmark tests
        compat: Compatibility tests
        security: Security audit tests
    ```

    Create `tests/windows/__init__.py` as empty file.

    Create `tests/windows/conftest.py` with these fixtures:

    1. `isolated_env` — saves `os.environ` dict copy on setup, restores on teardown. Use `yield` pattern with try/finally.
    2. `temp_run_dir` — creates `tmp_path / "ccb_run"` directory, sets `CCB_RUN_DIR` env var via monkeypatch, yields the path, cleans up on teardown.
    3. `daemon_spec` — returns a ProviderDaemonSpec-like object. Since we can't import lib.modules directly in tests, construct a simple object with attributes: `protocol_prefix="askd.test"`, `daemon_key="test-daemon"`, `lock_name="test-daemon.lock"`, `log_file_name="test-daemon.log"`, `idle_timeout_env="CCB_TEST_IDLE_TIMEOUT"`. Use `types.SimpleNamespace`.
    4. `daemon_token` — returns `"test-token-0123456789abcdef"` (fixed for deterministic tests).
    5. `free_port` — binds socket.socket() to port 0 on 127.0.0.1, gets actual port from `sock.getsockname()[1]`, closes socket, yields port.

    Import `pytest`, `os`, `socket`, `types` at top of conftest.py. Do NOT import lib modules in conftest — fixtures should be self-contained or accept spec as parameter.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/ --collect-only -q 2>&amp;1 | head -5</automated>
  </verify>
  <done>
    pytest.ini exists with [pytest] section and all four markers. `python -m pytest tests/windows/ --collect-only` runs without import errors. conftest.py has all 5 fixtures defined.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write daemon performance and lifecycle management tests (WIN-01)</name>
  <files>tests/windows/test_perf_daemon.py</files>
  <read_first>
    - tests/windows/test_perf_daemon.py (does not exist yet — create new)
    - lib/askd_server.py (AskDaemonServer.serve_forever() is the entry point)
    - lib/askd_runtime.py (run_dir(), random_token(), state_file_path())
    - lib/askd/daemon.py (UnifiedAskDaemon — daemon lifecycle: start/stop/cleanup, read_state, ping_daemon, shutdown_daemon)
    - lib/terminal.py (_subprocess_kwargs() for CREATE_NO_WINDOW flag on Windows)
  </read_first>
  <behavior>
    - Test 1: `test_daemon_cold_start_under_3s` — starts daemon subprocess with `sys.executable, "bin/askd"`, waits for state file to appear (poll every 100ms, timeout 5s), measures elapsed time, asserts < 3.0s. Uses CREATE_NO_WINDOW on Windows. Per D-06.
    - Test 2: `test_command_response_under_500ms` — if daemon is running from test 1, sends a ping via socket (read state file for port/token), measures round-trip, asserts < 0.5s. Per D-07. If daemon not running, start fresh.
    - Test 3: `test_daemon_memory_under_50mb` — if psutil is available, reads daemon process RSS after startup, asserts < 50*1024*1024 bytes. Per D-08. If psutil not available, pytest.mark.skip with reason.
    - Test 4: `test_python_module_import_time` — measures `timeit.timeit('import askd_server', setup='import sys; sys.path.insert(0, "lib")')`, reports duration (informational, no hard assert — baseline for future regression).
    - Test 5: `test_daemon_shutdown_cleans_state_file` — starts daemon, calls shutdown_daemon(), verifies state file is removed after shutdown. Per D-16 process management.
    - Test 6: `test_daemon_ping_returns_true_when_running` — starts daemon, calls ping_daemon(), asserts returns True.
    - Test 7: `test_daemon_ping_returns_false_when_not_running` — without daemon running (or with cleaned state), calls ping_daemon(), asserts returns False.
  </behavior>
  <action>
    Create `tests/windows/test_perf_daemon.py` with the following structure:

    All tests use `pytest.mark.windows` and `pytest.mark.perf` markers.

    **test_daemon_cold_start_under_3s:**
    - Record `start = time.perf_counter()`
    - Start subprocess: `subprocess.Popen([sys.executable, "bin/askd"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, creationflags=CREATE_NO_WINDOW if os.name=="nt" else 0)`
    - Poll for state file at `run_dir() / "askd.json"` every 100ms, up to 5s timeout
    - `elapsed = time.perf_counter() - start`
    - `assert elapsed < 3.0, f"daemon cold start took {elapsed:.2f}s (limit: 3s)"`
    - Store daemon proc and port in module-level variable for subsequent tests
    - Cleanup: `proc.terminate()` in fixture or teardown

    **test_command_response_under_500ms:**
    - Read state file to get port and token
    - Connect to `127.0.0.1:port` via `socket.create_connection()`, set 2s timeout
    - Send: `json.dumps({"type": "askd.ping", "v": 1, "id": "perf-test", "token": token}) + "\n"`
    - Read response line
    - `assert elapsed < 0.5, f"ping round-trip took {elapsed:.3f}s (limit: 500ms)"`

    **test_daemon_memory_under_50mb:**
    - Try `import psutil`
    - If ImportError: `pytest.skip("psutil not installed")`
    - Get daemon PID from state file
    - `rss = psutil.Process(pid).memory_info().rss`
    - `assert rss < 50 * 1024 * 1024, f"daemon RSS {rss / 1024 / 1024:.1f}MB (limit: 50MB)"`

    **test_python_module_import_time:**
    - Use `timeit.timeit(stmt='import askd_server', setup='import sys; sys.path.insert(0, "lib")', number=3)`
    - Log result with `pytest.mark.xfail(reason="informational only")` or just print to stdout
    - No assertion — this is a baseline measurement

    **test_daemon_shutdown_cleans_state_file (per D-16 process management):**
    - Import: `from lib.askd.daemon import shutdown_daemon, read_state`
    - Ensure daemon is running (start if needed using same subprocess pattern as test 1)
    - Get state_file path from `state_file_path("askd.json")` in temp_run_dir
    - Call `shutdown_daemon(state_file=temp_state_file, timeout_s=5.0)`
    - Assert returns True
    - Assert state file does NOT exist: `not temp_state_file.exists()`
    - This verifies daemon stop and cleanup per D-16 "进程管理" requirement

    **test_daemon_ping_returns_true_when_running:**
    - Import: `from lib.askd.daemon import ping_daemon`
    - Ensure daemon is running
    - Call `ping_daemon(timeout_s=2.0, state_file=temp_state_file)`
    - Assert returns True

    **test_daemon_ping_returns_false_when_not_running:**
    - Delete state file if it exists
    - Call `ping_daemon(timeout_s=1.0, state_file=temp_state_file)`
    - Assert returns False

    Important: All tests that interact with the real daemon must use `temp_run_dir` fixture to isolate from any existing daemon state. Use `isolated_env` fixture to prevent CCB_* env var leakage.

    The daemon startup subprocess should capture CCB_RUN_DIR env var pointing to temp directory. Add `env={**os.environ, "CCB_RUN_DIR": str(temp_dir)}` to Popen call.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/test_perf_daemon.py -x -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    All 7 test functions exist in test_perf_daemon.py. Tests that require daemon use temp_run_dir fixture. psutil-dependent test has skip condition. Daemon shutdown, ping, and state cleanup tests verify D-16 process management coverage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write socket communication and end-to-end command tests (WIN-01)</name>
  <files>tests/windows/test_perf_socket.py, tests/windows/test_e2e_commands.py</files>
  <read_first>
    - tests/windows/test_perf_socket.py (does not exist yet — create new)
    - tests/windows/test_e2e_commands.py (does not exist yet — create new)
    - lib/askd_server.py (Handler.handle() — socket message protocol)
    - lib/askd_client.py (client-side socket connection pattern)
    - lib/askd_rpc.py (RPC protocol format)
    - bin/ask (CLI entry point — command invocation pattern)
    - bin/pend (CLI entry point — poll for response)
    - lib/askd/daemon.py (ping_daemon, read_state — for E2E verification)
  </read_first>
  <behavior>
    test_perf_socket.py:
    - Test 1: `test_socket_connect_latency` — measures time from socket.create_connection() to successful connect on 127.0.0.1
    - Test 2: `test_socket_request_response_latency` — sends a ping message and measures full round-trip (write + read)
    - Test 3: `test_socket_unauthorized_rejected_fast` — sends request with wrong token, verifies response within 100ms and contains "Unauthorized"
    - Test 4: `test_concurrent_requests_handling` — sends 5 pings concurrently (threading), verifies all get responses

    test_e2e_commands.py:
    - Test 5: `test_e2e_daemon_start_creates_state_file` — runs `bin/askd` subprocess, verifies state file created with valid JSON containing "port", "token", "pid" keys
    - Test 6: `test_e2e_ask_ping_command` — runs `ask` CLI to send a ping message to running daemon, verifies response received without error
    - Test 7: `test_e2e_pend_returns_response` — runs `pend` CLI to poll for a previously submitted task response, verifies it returns a response dict or timeout gracefully
    - Test 8: `test_e2e_ccb_start_stop_cycle` — runs daemon start, verifies state file exists, then stops daemon, verifies state file cleaned up — full lifecycle test
  </behavior>
  <action>
    Create `tests/windows/test_perf_socket.py` with Tests 1-4:

    **test_socket_connect_latency:**
    - Read state file (port, token) using temp_run_dir fixture
    - `start = time.perf_counter()`, `sock = socket.create_connection(("127.0.0.1", port), timeout=2)`, `elapsed = time.perf_counter() - start`
    - `assert elapsed < 0.1, f"socket connect took {elapsed:.3f}s"`
    - Close socket

    **test_socket_request_response_latency:**
    - Connect, send ping with correct token, measure round-trip
    - `assert elapsed < 0.5`

    **test_socket_unauthorized_rejected_fast:**
    - Connect, send request with token="wrong-token"
    - Measure time to receive response
    - `assert elapsed < 0.1`
    - Parse response JSON, verify `reply == "Unauthorized"`

    **test_concurrent_requests_handling:**
    - Use `concurrent.futures.ThreadPoolExecutor(max_workers=5)` to send 5 pings simultaneously
    - Each thread: connect, send ping, read response, record elapsed time
    - Assert all 5 received responses
    - Assert max individual latency < 1.0s

    All socket tests use `pytest.mark.windows` and `pytest.mark.perf`. Tests depend on a running daemon — use a module-scoped fixture or `request.getfixturevalue` to share daemon state with test_perf_daemon.py. If daemon is not available, tests should `pytest.skip("daemon not running")`.

    Use `conftest.py` fixtures: `isolated_env`, `temp_run_dir`, `daemon_token`.

    Create `tests/windows/test_e2e_commands.py` with Tests 5-8:

    All E2E tests use `pytest.mark.windows` markers. These tests verify Success Criteria #1: "原生 Windows 环境下所有核心命令（ask/pend/ccb start 等）功能正常".

    **test_e2e_daemon_start_creates_state_file:**
    - Start daemon subprocess with temp_run_dir: `subprocess.Popen([sys.executable, "bin/askd"], env={**os.environ, "CCB_RUN_DIR": str(temp_dir)}, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, creationflags=CREATE_NO_WINDOW if os.name=="nt" else 0)`
    - Poll for state file at `temp_dir / "askd.json"` every 200ms, timeout 5s
    - Read state file: `json.loads(state_file.read_text())`
    - Assert `"port"` in state, `"token"` in state, `"pid"` in state
    - Assert `state["pid"]` is a positive integer
    - Cleanup: terminate daemon process

    **test_e2e_ask_ping_command:**
    - Ensure daemon is running (from test 5 or start fresh)
    - Read state file to get port and token
    - Connect via socket, send ping: `{"type": "askd.ping", "v": 1, "id": "e2e-test", "token": token}`
    - Read response, assert no error in reply
    - Assert response contains `"type"` field with value ending in "response"

    **test_e2e_pend_returns_response:**
    - This test simulates the pend workflow: submit a task, then poll for response
    - Since pend requires an actual AI provider, test the polling mechanism only:
    - Read state file for port/token
    - Connect via socket, send a minimal request with no provider (will get error response)
    - This verifies the command -> socket -> daemon -> response pipeline works end-to-end
    - Alternatively: test `bin/pend` script exists and is executable: `assert Path("bin/pend").exists()`
    - And test it returns a parseable exit code when no task is pending

    **test_e2e_ccb_start_stop_cycle:**
    - Start daemon (same as test 5)
    - Verify state file exists with valid JSON
    - Call `shutdown_daemon(state_file=temp_state_file, timeout_s=5.0)` or terminate process
    - Wait up to 3s for process to exit
    - Assert state file is cleaned up
    - This is the full lifecycle: start -> verify running -> stop -> verify cleanup

    All E2E tests use `temp_run_dir` fixture. If daemon binary (`bin/askd`) does not exist, tests should `pytest.skip("bin/askd not found")`.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/test_perf_socket.py tests/windows/test_e2e_commands.py -x -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    4 socket tests + 4 E2E tests = 8 total. Unauthorized test verifies rejection within 100ms. Concurrent test sends 5 simultaneous pings. E2E tests verify daemon start, ping command, pend polling, and full start/stop lifecycle on Windows. Addresses Success Criteria #1 and D-16.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/windows/test_perf_daemon.py tests/windows/test_perf_socket.py tests/windows/test_e2e_commands.py -x -v` runs all 15 tests
2. pytest.ini is at project root with correct [pytest] section
3. `tests/windows/conftest.py` defines isolated_env, temp_run_dir, daemon_spec, daemon_token, free_port
4. Performance tests measure actual values and compare against D-06/D-07/D-08 thresholds
5. Daemon lifecycle tests verify shutdown, ping, and state cleanup (D-16 process management)
6. E2E tests verify core commands (ask, pend, ccb start) work end-to-end (Success Criteria #1)
</verification>

<success_criteria>
- pytest can discover tests in tests/windows/ with zero configuration changes
- Daemon cold start is benchmarked against 3s threshold
- Command response time is benchmarked against 500ms threshold
- Daemon memory usage is benchmarked against 50MB threshold
- Socket communication latency is measured
- All performance tests produce quantifiable data (pass/fail with measured values in assertion messages)
- Daemon shutdown and state cleanup verified (D-16 process management)
- Core commands (ask/pend/ccb start) verified end-to-end on Windows (Success Criteria #1)
</success_criteria>

<output>
After completion, create `.planning/phases/07-windows-native-audit/07-01-SUMMARY.md`
</output>
