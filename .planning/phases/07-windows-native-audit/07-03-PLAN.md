---
phase: 07-windows-native-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/windows/test_security_token.py
  - tests/windows/test_security_permission.py
  - tests/windows/test_security_process.py
  - tests/windows/test_security_socket.py
autonomous: true
requirements: [WIN-03]
user_setup: []

must_haves:
  truths:
    - "daemon state file token is not logged to any log file (D-11/D-12)"
    - "os.chmod(0o600) has no effect on Windows NTFS — verified by test (Pitfall 1)"
    - "No eval() or exec() calls exist in the entire lib/ codebase (D-12)"
    - "No subprocess calls use shell=True in the entire lib/ and bin/ codebase (D-12)"
    - "Socket server requires token authentication — requests without correct token are rejected (D-11)"
    - "Token comparison is not timing-safe — documented as low-risk audit finding (D-11)"
    - "Daemon state file stores token as plaintext — documented as security finding (Pitfall 3)"
  artifacts:
    - path: "tests/windows/test_security_token.py"
      provides: "token handling and leakage tests"
      contains: "def test_token"
    - path: "tests/windows/test_security_permission.py"
      provides: "file permission tests for Windows NTFS"
      contains: "def test_chmod"
    - path: "tests/windows/test_security_process.py"
      provides: "process security tests (eval/exec/shell=True audit)"
      contains: "def test_no_eval"
    - path: "tests/windows/test_security_socket.py"
      provides: "socket authentication and security tests"
      contains: "def test_socket_auth"
  key_links:
    - from: "tests/windows/test_security_token.py"
      to: "lib/askd_runtime.py"
      via: "import random_token; verify token entropy"
      pattern: "from lib.askd_runtime import random_token"
    - from: "tests/windows/test_security_permission.py"
      to: "lib/askd_server.py"
      via: "verify _write_state chmod behavior on Windows"
      pattern: "os.chmod"
    - from: "tests/windows/test_security_process.py"
      to: "lib/"
      via: "grep scan of all lib/*.py for eval/exec/shell=True"
      pattern: "eval\\(|exec\\(|shell=True"
    - from: "tests/windows/test_security_socket.py"
      to: "lib/askd_server.py"
      via: "Handler.handle() token verification"
      pattern: "msg.get.*token"
---

<objective>
Write comprehensive security audit tests covering token handling, file permissions, process security, and socket authentication.

Purpose: Verify token is not leaked to logs (D-11/D-12), document os.chmod(0o600) ineffectiveness on NTFS (Pitfall 1), scan entire codebase for eval/exec/shell=True usage (D-12), validate socket token authentication (D-11), and identify timing-unsafe token comparison.

Output: test_security_token.py, test_security_permission.py, test_security_process.py, test_security_socket.py
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-windows-native-audit/07-CONTEXT.md
@.planning/phases/07-windows-native-audit/07-RESEARCH.md

<interfaces>
From lib/askd_runtime.py:
```python
def random_token() -> str: ...     # returns os.urandom(16).hex() — 32 hex chars
def run_dir() -> Path: ...         # %LOCALAPPDATA%/ccb on Windows
def state_file_path(name: str) -> Path: ...
def write_log(path: Path, msg: str) -> None: ...  # appends msg to log file
```

From lib/askd_server.py (Handler.handle, lines 104-120):
```python
class Handler(socketserver.StreamRequestHandler):
    def handle(self) -> None:
        # ...
        if msg.get("token") != self.server.token:  # line 118 — timing-unsafe comparison
            self._write({"type": response_type, ..., "reply": "Unauthorized"})
            return
```

From lib/askd_server.py (_write_state, lines 281-301):
```python
def _write_state(self, host: str, port: int) -> None:
    payload = {
        "token": self.token,  # plaintext in JSON — Pitfall 3
        # ...
    }
    # chmod only on non-Windows:
    if os.name != "nt":
        os.chmod(self.state_file, 0o600)
```

Key security research findings (from RESEARCH.md):
- No shell=True found in entire codebase (grep verified)
- No eval/exec found in lib/ codebase (grep verified)
- 12 os.chmod(0o600) calls found: 7 in mail/, 1 in askd_server.py (guarded), 4 elsewhere
- Token stored as plaintext in askd.json
- Socket token comparison uses `!=` (not timing-safe) — low risk on localhost
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write token handling and leakage tests (WIN-03 D-11/D-12)</name>
  <files>tests/windows/test_security_token.py</files>
  <read_first>
    - tests/windows/test_security_token.py (does not exist yet — create new)
    - lib/askd_runtime.py (random_token(), write_log(), run_dir())
    - lib/askd_server.py (_write_state() — token written to state file, lines 281-301)
    - lib/session_utils.py (safe_write_session — how files are written)
  </read_first>
  <behavior>
    - Test 1: `test_random_token_entropy` — random_token() returns 32 hex chars (128 bits of entropy)
    - Test 2: `test_random_token_unique` — two calls to random_token() return different values
    - Test 3: `test_token_in_state_file_is_plaintext` — state file JSON contains token as plaintext string (audit finding: document, not fix in this test)
    - Test 4: `test_token_not_in_log_files` — after writing logs via write_log(), token value does not appear in log content
    - Test 5: `test_token_not_exposed_in_error_messages` — simulated error paths in askd_server.py Handler do not include token in error replies
  </behavior>
  <action>
    Create `tests/windows/test_security_token.py` with all 5 tests.

    Import pattern:
    ```python
    import json, os, secrets
    import pytest
    from pathlib import Path
    from lib.askd_runtime import random_token, write_log, run_dir
    ```

    **test_random_token_entropy:**
    - `token = random_token()`
    - `assert len(token) == 32` — 16 bytes = 32 hex chars
    - `assert all(c in "0123456789abcdef" for c in token)` — all hex

    **test_random_token_unique:**
    - `t1, t2 = random_token(), random_token()`
    - `assert t1 != t2` — statistical near-certainty

    **test_token_in_state_file_is_plaintext:**
    - Create temp state file, write JSON with token
    - `state = {"token": "test-secret-123", "port": 12345, "pid": 1234}`
    - Write to temp file
    - Read back and verify `"test-secret-123"` appears as-is (not hashed/encrypted)
    - `assert json.loads(content)["token"] == "test-secret-123"`
    - This is an audit finding: `pytest.mark.xfail(reason="Pitfall 3: token stored as plaintext — needs DACL or DPAPI fix")`
    - Actually, don't xfail — this is expected behavior for audit documentation. Just assert and annotate.

    **test_token_not_in_log_files:**
    - Create temp log file using `tmp_path / "test.log"`
    - Set `test_token = "audit-test-token-abc123"`
    - Call `write_log(tmp_path / "test.log", f"[INFO] daemon started pid={os.getpid()}")`
    - Call `write_log(tmp_path / "test.log", f"[ERROR] connection failed")`
    - `content = (tmp_path / "test.log").read_text(encoding="utf-8")`
    - `assert test_token not in content`
    - Also verify token-like strings (32 hex chars) are not in log content:
      `import re; assert not re.search(r'\b[0-9a-f]{32}\b', content)`

    **test_token_not_exposed_in_error_messages:**
    - Read lib/askd_server.py Handler.handle() code
    - Verify that error response dicts (lines 119, 133, 143, 149) do NOT include `self.server.token`
    - Use AST parsing: parse askd_server.py, walk all dict literals in Handler class, check none contain key "token"
    - Simpler approach: grep the file for patterns where token appears in response dicts
    - `assert "self.server.token" not in error_response_code` — where error_response_code is the extracted error handling section

    All tests: `@pytest.mark.windows`, `@pytest.mark.security`.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/test_security_token.py -x -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    5 test functions defined. Token entropy and uniqueness verified. Plaintext storage documented. Log leakage test verifies write_log does not include token.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write file permission and process security tests (WIN-03 D-11/D-12)</name>
  <files>tests/windows/test_security_permission.py, tests/windows/test_security_process.py</files>
  <read_first>
    - tests/windows/test_security_permission.py (does not exist yet — create new)
    - tests/windows/test_security_process.py (does not exist yet — create new)
    - lib/askd_server.py (_write_state chmod guard at line 297 — correct pattern)
    - lib/mail/daemon.py (chmod without Windows guard — Pitfall 1)
    - lib/mail/credentials.py (chmod without Windows guard — Pitfall 1)
    - lib/session_utils.py (any chmod calls)
  </read_first>
  <behavior>
    test_security_permission.py:
    - Test 1: `test_chmod_600_ineffective_on_ntfs` — create temp file, chmod 0o600, verify st_mode does NOT reflect 0o600 on Windows
    - Test 2: `test_chmod_400_sets_readonly_on_windows` — create temp file, chmod 0o400, verify file becomes read-only (os.access with W_OK)
    - Test 3: `test_askd_server_chmod_has_nt_guard` — verify lib/askd_server.py _write_state has `os.name != "nt"` guard before chmod
    - Test 4: `test_mail_modules_missing_nt_guard` — scan lib/mail/*.py for chmod calls WITHOUT os.name nt guard (audit finding: document the gap)

    test_security_process.py:
    - Test 5: `test_no_eval_in_lib` — grep all lib/*.py files, no `eval(` calls (excluding comments and strings)
    - Test 6: `test_no_exec_in_lib` — grep all lib/*.py files, no `exec(` calls (excluding comments and strings)
    - Test 7: `test_no_shell_true_in_lib` — grep all lib/*.py and bin/* files, no `shell=True` in subprocess calls
    - Test 8: `test_no_hardcoded_secrets_in_lib` — grep lib/*.py for patterns like "password=", "secret=", "api_key=" with string literals (not env vars)
  </behavior>
  <action>
    Create `tests/windows/test_security_permission.py` with 4 tests:

    Import pattern:
    ```python
    import os, glob, re
    import stat as stat_mod
    import pytest
    from pathlib import Path
    ```

    **test_chmod_600_ineffective_on_ntfs:**
    - `test_file = tmp_path / "chmod_test.txt"`
    - `test_file.write_text("sensitive data")`
    - `os.chmod(test_file, 0o600)`
    - `st = test_file.stat()`
    - `actual_mode = stat_mod.S_IMODE(st.st_mode) & 0o777`
    - On Windows: `actual_mode` will be 0o666 (NTFS default), NOT 0o600
    - `if os.name == "nt": assert actual_mode != 0o600, "os.chmod(0o600) should be ineffective on NTFS (Pitfall 1)"`
    - On Unix: `assert actual_mode == 0o600`

    **test_chmod_400_sets_readonly_on_windows:**
    - `test_file = tmp_path / "readonly_test.txt"`
    - `test_file.write_text("data")`
    - `os.chmod(test_file, 0o400)`
    - On Windows: `assert not os.access(test_file, os.W_OK)` — 0o400 sets read-only flag on Windows
    - Cleanup: restore write access for tmp_path cleanup

    **test_askd_server_chmod_has_nt_guard:**
    - Read `lib/askd_server.py` as text
    - Find the chmod call (line ~299): `os.chmod(self.state_file, 0o600)`
    - Verify it is inside `if os.name != "nt":` block
    - Use regex or string search: find `os.chmod`, then check that preceding lines (within 5 lines) contain `os.name != "nt"` or `sys.platform != "win32"`
    - `assert "os.name" in guard_context` — the guard exists

    **test_mail_modules_missing_nt_guard:**
    - Glob `lib/mail/*.py`
    - For each file, grep for `chmod` calls
    - Check if the chmod call is preceded by an `os.name != "nt"` or `sys.platform != "win32"` guard
    - Collect files that have chmod WITHOUT guard
    - This is an audit finding: assert the list is empty (it won't be — this documents the gap)
    - Use `pytest.mark.xfail(reason="Pitfall 1: mail/ modules have unguarded chmod on Windows")`
    - Or: `pytest.warns(UserWarning)` to document

    Create `tests/windows/test_security_process.py` with 4 tests:

    Import pattern:
    ```python
    import os, glob, re
    import pytest
    from pathlib import Path
    ```

    **test_no_eval_in_lib:**
    - `lib_files = glob.glob("lib/*.py")`
    - For each file, read content, strip comments (lines starting with #)
    - Use regex: `r'\beval\s*\('` — no matches allowed
    - Allow `eval` in string literals (docstrings, error messages): use AST parsing
    - Simpler approach: `re.search(r'^[^#]*\beval\s*\(', content, re.MULTILINE)` for each file
    - `assert not found` — no eval calls in executable code

    **test_no_exec_in_lib:**
    - Same pattern as above but for `r'\bexec\s*\('`

    **test_no_shell_true_in_lib:**
    - Scan `lib/*.py` and `bin/*` (Python files in bin/)
    - Regex: `r'shell\s*=\s*True'`
    - `assert not found`

    **test_no_hardcoded_secrets_in_lib:**
    - Scan `lib/*.py` for patterns: `r'(?i)(password|secret|api_key|apikey)\s*=\s*["\'][^"\']+["\']'`
    - Exclude: env var lookups (`os.environ`), function parameters, variable assignments from env
    - This is a loose scan — document any false positives
    - `assert not found`

    All tests: `@pytest.mark.windows`, `@pytest.mark.security`.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/test_security_permission.py tests/windows/test_security_process.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    4 permission tests + 4 process tests = 8 total. chmod NTFS ineffectiveness documented. mail/ unguarded chmod identified. eval/exec/shell=True absence verified by codebase scan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write socket security audit tests (WIN-03 D-11)</name>
  <files>tests/windows/test_security_socket.py</files>
  <read_first>
    - tests/windows/test_security_socket.py (does not exist yet — create new)
    - lib/askd_server.py (Handler.handle() lines 104-149 — token verification, request routing)
    - lib/askd_client.py (client-side socket connection and authentication)
  </read_first>
  <behavior>
    - Test 1: `test_socket_rejects_missing_token` — request without token field returns "Unauthorized"
    - Test 2: `test_socket_rejects_wrong_token` — request with incorrect token returns "Unauthorized"
    - Test 3: `test_socket_accepts_correct_token` — request with correct token gets processed (pong response)
    - Test 4: `test_socket_token_comparison_not_timing_safe` — verify the comparison uses Python `!=` (document as low-risk finding, not a fix)
    - Test 5: `test_socket_no_tls_encryption` — socket connection uses plain TCP (no SSL/TLS) — document as audit finding
    - Test 6: `test_socket_binds_localhost_only` — server binds to 127.0.0.1, not 0.0.0.0 (verify from code, not runtime)
  </behavior>
  <action>
    Create `tests/windows/test_security_socket.py` with all 6 tests.

    Import pattern:
    ```python
    import json, os, socket, ssl
    import pytest
    from pathlib import Path
    ```

    **test_socket_rejects_missing_token:**
    - Read state file for port (or use free_port + start mini server)
    - Connect to daemon socket
    - Send: `json.dumps({"type": "askd.ping", "v": 1, "id": "sec-test-1"}) + "\n"` (no token field)
    - Read response
    - `assert response["reply"] == "Unauthorized"`

    **test_socket_rejects_wrong_token:**
    - Same but with `"token": "wrong-token-value"`
    - `assert response["reply"] == "Unauthorized"`

    **test_socket_accepts_correct_token:**
    - Read actual token from state file
    - Send ping with correct token
    - `assert response["exit_code"] == 0`
    - `assert "pong" in response["type"]`

    **test_socket_token_comparison_not_timing_safe:**
    - Read lib/askd_server.py
    - Find the token comparison line: `if msg.get("token") != self.server.token:`
    - Verify it uses `!=` operator (not `hmac.compare_digest`)
    - Use regex on the file content
    - `assert "!=" in comparison_line and "compare_digest" not in comparison_line`
    - This is an audit finding: `pytest.mark.xfail(reason="Known: token comparison uses != not hmac.compare_digest — low risk on localhost")`
    - Actually, don't xfail — assert the current behavior and annotate

    **test_socket_no_tls_encryption:**
    - Read lib/askd_server.py Server class
    - Verify it uses `socketserver.ThreadingTCPServer` (not SSL-wrapped)
    - Verify no `ssl.wrap_socket` or `ssl.SSLContext` in the file
    - `assert "ssl" not in server_code.lower()` or `assert "SSLContext" not in server_code`
    - Audit finding: document as expected (localhost-only daemon, low risk)

    **test_socket_binds_localhost_only:**
    - Read lib/askd_server.py constructor: default `host="127.0.0.1"`
    - Also check AskDaemonServer.__init__ default parameter
    - `assert "127.0.0.1" in source` for the host parameter default
    - Verify `normalize_connect_host()` converts "0.0.0.0" to "127.0.0.1" (from askd_runtime.py)

    Tests 1-3 require a running daemon. Use same pattern as test_perf_socket.py: read state file for port/token. If daemon not running, `pytest.skip("daemon not running")`.

    Tests 4-6 are static code analysis — no daemon needed.

    All tests: `@pytest.mark.windows`, `@pytest.mark.security`.
  </action>
  <verify>
    <automated>python -m pytest tests/windows/test_security_socket.py -x -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    6 test functions defined. Token rejection (missing + wrong) verified. Timing-unsafe comparison documented. No TLS documented. Localhost binding verified from code.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/windows/test_security_token.py tests/windows/test_security_permission.py tests/windows/test_security_process.py tests/windows/test_security_socket.py -x -v` runs all 23 tests
2. Token tests verify no leakage to logs
3. Permission tests document NTFS chmod ineffectiveness (Pitfall 1)
4. Process tests scan entire codebase for eval/exec/shell=True (D-12)
5. Socket tests verify authentication mechanism and document missing TLS
</verification>

<success_criteria>
- All 23 security tests run without import errors
- Token entropy (128-bit) and uniqueness verified
- os.chmod(0o600) ineffectiveness on NTFS is tested and documented
- mail/ modules with unguarded chmod calls are identified
- Complete codebase scan confirms no eval/exec/shell=True
- Socket token rejection tested (missing token, wrong token)
- Timing-unsafe comparison and missing TLS documented as audit findings
</success_criteria>

<output>
After completion, create `.planning/phases/07-windows-native-audit/07-03-SUMMARY.md`
</output>
