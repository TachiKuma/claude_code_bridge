---
phase: 04-原型验证
plan: 03
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - scripts/check_protocol_strings.py
  - lib/i18n_core.py
  - tests/test_protocol_check.py
  - tests/fixtures/bad_translations/zh_bad.json
autonomous: true
requirements: [PROTO-03]
user_setup: []

must_haves:
  truths:
    - "CI 检查脚本可扫描 lib/i18n/**/*.json 中的所有翻译值"
    - "翻译值包含协议字符串时检查脚本报告错误并以非零退出码退出"
    - "正常翻译文件通过检查，脚本以 0 退出码退出"
    - "i18n_core 加载翻译时可验证翻译值不包含协议字符串（运行时验证）"
    - "白名单从 .planning/protocol_whitelist.json 加载，包含 300 个条目"
  artifacts:
    - path: "scripts/check_protocol_strings.py"
      provides: "CI 检查脚本，对照白名单检查翻译值"
      min_lines: 60
    - path: "tests/test_protocol_check.py"
      provides: "协议检查脚本和运行时验证的单元测试"
      min_lines: 50
    - path: "tests/fixtures/bad_translations/zh_bad.json"
      provides: "测试用含协议字符串的错误翻译文件"
  key_links:
    - from: "scripts/check_protocol_strings.py"
      to: ".planning/protocol_whitelist.json"
      via: "json.load() 加载白名单"
      pattern: "protocol_whitelist\\.json"
    - from: "lib/i18n_core.py"
      to: ".planning/protocol_whitelist.json"
      via: "load_translations() 时验证翻译值"
      pattern: "protocol_whitelist"
---

<objective>
实现协议字符串保护机制：CI 检查脚本 + i18n_core 运行时验证。

Purpose: 验证双层保护策略（D-10）可有效防止协议字符串被错误翻译（PROTO-03）。
Output: CI 检查脚本 + i18n_core 运行时验证集成 + 测试
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-原型验证/04-CONTEXT.md
@.planning/phases/02-架构设计/designs/protocol_protection_design.md
@.planning/protocol_whitelist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI 检查脚本 + i18n_core 运行时验证 + 测试</name>
  <files>scripts/check_protocol_strings.py, lib/i18n_core.py, tests/test_protocol_check.py, tests/fixtures/bad_translations/zh_bad.json</files>
  <read_first>
    - .planning/protocol_whitelist.json（白名单，300 个协议字符串，7 个分类）
    - .planning/phases/02-架构设计/designs/protocol_protection_design.md（双层保护设计，CI 脚本核心逻辑，检查范围）
    - lib/i18n_core.py（需要添加运行时验证逻辑）
    - lib/i18n/ccb/zh.json（Plan 01 创建的翻译文件，CI 脚本需要扫描的目标）
  </read_first>
  <action>
    **1. 创建 `scripts/check_protocol_strings.py`：**

    按照 protocol_protection_design.md Section 3 实现核心逻辑，并增加命令行参数支持：

    ```python
    #!/usr/bin/env python3
    """
    check_protocol_strings.py - 检查翻译文件中是否包含协议字符串

    对照 .planning/protocol_whitelist.json 白名单检查翻译值。
    这是协议保护的第一层：CI 值检查。

    用法:
        python scripts/check_protocol_strings.py [--values] [--code] [--whitelist PATH] [--translations PATH]

    退出码:
        0 - 检查通过
        1 - 发现违规
    """
    import argparse
    import json
    import sys
    from pathlib import Path
    from typing import Set, List, Dict, Any

    DEFAULT_WHITELIST = Path(__file__).parent.parent / ".planning" / "protocol_whitelist.json"
    DEFAULT_TRANSLATION_DIR = Path(__file__).parent.parent / "lib" / "i18n"

    def load_whitelist(path: Path) -> Set[str]:
        """加载协议字符串白名单"""
        with open(path, encoding="utf-8") as f:
            data = json.load(f)
        strings = set()
        for category, items in data.get("categories", {}).items():
            strings.update(items)
        return strings

    def check_translation_file(file_path: Path, whitelist: Set[str]) -> List[Dict[str, str]]:
        """检查单个翻译文件的值是否包含协议字符串"""
        errors = []
        try:
            with open(file_path, encoding="utf-8") as f:
                translations = json.load(f)
        except json.JSONDecodeError as e:
            return [{"type": "parse_error", "file": str(file_path), "error": str(e)}]

        for key, value in translations.items():
            if isinstance(value, str) and value in whitelist:
                errors.append({
                    "type": "protocol_value",
                    "file": str(file_path),
                    "key": key,
                    "value": value,
                    "reason": "Protocol string should not be a translation value"
                })
        return errors

    def scan_translation_values(translation_dir: Path, whitelist: Set[str]) -> List[Dict[str, str]]:
        """扫描翻译目录中所有 JSON 文件"""
        all_errors = []
        for json_file in sorted(translation_dir.rglob("*.json")):
            errors = check_translation_file(json_file, whitelist)
            all_errors.extend(errors)
        return all_errors

    def main():
        parser = argparse.ArgumentParser(description="Check protocol strings in translation files")
        parser.add_argument("--values", action="store_true", default=True,
                          help="Check translation values (default)")
        parser.add_argument("--whitelist", type=Path, default=DEFAULT_WHITELIST,
                          help="Path to protocol whitelist JSON")
        parser.add_argument("--translations", type=Path, default=DEFAULT_TRANSLATION_DIR,
                          help="Path to translation directory")
        args = parser.parse_args()

        # 1. 加载白名单
        try:
            whitelist = load_whitelist(args.whitelist)
        except (FileNotFoundError, json.JSONDecodeError) as e:
            print(f"FAIL: Cannot load whitelist: {e}", file=sys.stderr)
            sys.exit(1)

        print(f"Loaded {len(whitelist)} protocol strings from whitelist")

        # 2. 检查翻译值
        if args.values:
            errors = scan_translation_values(args.translations, whitelist)
            if errors:
                print(f"\nFAIL: Found {len(errors)} protocol string violation(s):")
                for err in errors:
                    if err.get("type") == "protocol_value":
                        print(f"  {err['file']}: key '{err['key']}' has protocol value '{err['value']}'")
                    else:
                        print(f"  {err['file']}: {err.get('error', err.get('reason', 'unknown'))}")
                sys.exit(1)
            else:
                print(f"PASS: No protocol strings found in {len(list(args.translations.rglob('*.json')))} translation file(s)")
        sys.exit(0)

    if __name__ == "__main__":
        main()
    ```

    **2. 修改 `lib/i18n_core.py` 添加运行时验证：**

    在 `load_translations()` 方法末尾添加白名单验证逻辑（双层保护的第二层）：

    在 I18nCore 类中添加私有方法 `_validate_no_protocol_strings()`：
    ```python
    def _validate_no_protocol_strings(self) -> List[str]:
        """验证翻译值不包含协议字符串（运行时保护第二层）

        Returns:
            违规列表，每项为 (key, value) 元组
        """
        violations = []
        whitelist = self._load_whitelist()
        if not whitelist:
            return violations

        for key, value in self.translations.items():
            if isinstance(value, str) and value in whitelist:
                violations.append(f"Key '{key}' has protocol string value: '{value}'")
                self.logger.warning(f"Protocol string violation: key='{key}', value='{value}'")

        return violations

    def _load_whitelist(self) -> Set[str]:
        """加载协议白名单"""
        # 白名单位置: 项目根目录/.planning/protocol_whitelist.json
        whitelist_path = Path(__file__).parent.parent / ".planning" / "protocol_whitelist.json"
        if not whitelist_path.exists():
            self.logger.debug(f"Protocol whitelist not found at {whitelist_path}")
            return set()
        try:
            with open(whitelist_path, encoding="utf-8") as f:
                data = json.load(f)
            strings = set()
            for category, items in data.get("categories", {}).items():
                strings.update(items)
            return strings
        except (json.JSONDecodeError, OSError) as e:
            self.logger.warning(f"Failed to load protocol whitelist: {e}")
            return set()
    ```

    在 `load_translations()` 方法中调用验证：
    ```python
    def load_translations(self) -> None:
        # ... 现有加载逻辑 ...

        # 运行时协议验证
        violations = self._validate_no_protocol_strings()
        if violations:
            self.logger.warning(
                f"Protocol string violations detected: {len(violations)}. "
                f"Review translations and .planning/protocol_whitelist.json"
            )
    ```

    注意：运行时验证只记录警告日志，不抛异常（per D-10，双层保护仅 CI 层阻止合并）。

    **3. 创建测试用错误翻译文件 `tests/fixtures/bad_translations/zh_bad.json`：**

    ```json
    {
      "ccb.error.test_protocol_env": "CCB_LANG",
      "ccb.error.test_protocol_cmd": "ask",
      "ccb.error.test_protocol_marker": "CCB_DONE",
      "ccb.error.normal": "这是一个正常的翻译"
    }
    ```

    **4. 创建 `tests/test_protocol_check.py`：**

    ```python
    import unittest
    import json
    import tempfile
    import os
    from pathlib import Path
    from scripts.check_protocol_strings import (
        load_whitelist,
        check_translation_file,
        scan_translation_values,
    )

    class TestLoadWhitelist(unittest.TestCase):
        def test_load_real_whitelist(self):
            """加载实际白名单文件"""
            whitelist_path = Path(".planning/protocol_whitelist.json")
            whitelist = load_whitelist(whitelist_path)
            self.assertGreater(len(whitelist), 100)
            self.assertIn("CCB_LANG", whitelist)
            self.assertIn("ask", whitelist)
            self.assertIn("CCB_DONE", whitelist)

    class TestCheckTranslationFile(unittest.TestCase):
        def test_clean_translation_passes(self):
            """正常翻译文件通过检查"""
            with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False, encoding='utf-8') as f:
                json.dump({"ccb.error.normal": "This is a normal message"}, f)
                f.flush()
                errors = check_translation_file(Path(f.name), {"CCB_LANG", "ask"})
            os.unlink(f.name)
            self.assertEqual(len(errors), 0)

        def test_protocol_value_detected(self):
            """协议字符串值被检测"""
            with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False, encoding='utf-8') as f:
                json.dump({"ccb.error.bad": "CCB_LANG", "ccb.error.good": "Normal message"}, f)
                f.flush()
                errors = check_translation_file(Path(f.name), {"CCB_LANG", "ask"})
            os.unlink(f.name)
            self.assertEqual(len(errors), 1)
            self.assertEqual(errors[0]["key"], "ccb.error.bad")
            self.assertEqual(errors[0]["value"], "CCB_LANG")

        def test_bad_fixture_detected(self):
            """测试用错误翻译文件被正确检测"""
            whitelist = load_whitelist(Path(".planning/protocol_whitelist.json"))
            errors = check_translation_file(
                Path("tests/fixtures/bad_translations/zh_bad.json"),
                whitelist
            )
            # 应该检测到 3 个违规（CCB_LANG, ask, CCB_DONE）
            self.assertGreaterEqual(len(errors), 3)

    class TestScanTranslationValues(unittest.TestCase):
        def test_scan_with_bad_fixture(self):
            """扫描目录时包含错误文件"""
            whitelist = load_whitelist(Path(".planning/protocol_whitelist.json"))
            errors = scan_translation_values(
                Path("tests/fixtures/bad_translations"),
                whitelist
            )
            self.assertGreater(len(errors), 0)

        def test_scan_clean_directory(self):
            """扫描干净目录不报错"""
            with tempfile.TemporaryDirectory() as tmpdir:
                good_file = Path(tmpdir) / "en.json"
                good_file.write_text(
                    json.dumps({"ccb.error.normal": "Normal message"}),
                    encoding="utf-8"
                )
                errors = scan_translation_values(Path(tmpdir), {"CCB_LANG"})
                self.assertEqual(len(errors), 0)
    ```

    注意：本 plan 不依赖 Plan 01（翻译文件可以独立测试用 fixtures）。CI 脚本的默认扫描路径指向 `lib/i18n/`，但测试通过 fixtures 覆盖所有场景。
  </action>
  <verify>
    <automated>cd "E:/GitHub开源项目/TachiKuma/claude_code_bridge" && python -m pytest tests/test_protocol_check.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - scripts/check_protocol_strings.py 存在且包含 `def main()` 和 `def load_whitelist()`
    - 脚本可执行 `python scripts/check_protocol_strings.py` 并输出白名单加载计数
    - `python scripts/check_protocol_strings.py --translations tests/fixtures/bad_translations` 以非零退出码退出
    - lib/i18n_core.py 包含 `_validate_no_protocol_strings()` 方法
    - lib/i18n_core.py 包含 `_load_whitelist()` 方法
    - tests/test_protocol_check.py 包含至少 4 个测试用例
    - tests/fixtures/bad_translations/zh_bad.json 包含 `"CCB_LANG"` 和 `"ask"` 作为翻译值
    - `python -m pytest tests/test_protocol_check.py` 全部通过
  </done>
</task>

</tasks>

<verification>
1. `python scripts/check_protocol_strings.py --translations tests/fixtures/bad_translations` 以非零退出码退出并报告违规
2. `python scripts/check_protocol_strings.py --whitelist .planning/protocol_whitelist.json` 扫描 lib/i18n/ 目录（若 Plan 01 已完成，应通过）
3. `python -m pytest tests/test_protocol_check.py -v` 全部通过
4. `python -c "from lib.i18n_core import I18nCore; i = I18nCore('ccb'); print(hasattr(i, '_validate_no_protocol_strings'))"` 返回 True
</verification>

<success_criteria>
- CI 检查脚本可加载白名单（300 项）并扫描翻译文件
- 协议字符串值被准确检测，正常翻译不误报
- i18n_core 加载翻译时自动验证，违规记录到日志
- 测试覆盖：白名单加载、正常翻译、协议违规、批量扫描
- 脚本支持 --translations 参数指定扫描目录
</success_criteria>

<output>
After completion, create `.planning/phases/04-原型验证/04-03-SUMMARY.md`
</output>
