---
phase: 04-原型验证
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n_core.py
  - lib/i18n/ccb/en.json
  - lib/i18n/ccb/zh.json
  - lib/i18n/ccb/xx.json
  - lib/i18n.py
  - tests/test_i18n_core.py
  - tests/demo_i18n_core.py
autonomous: true
requirements: [PROTO-01]
user_setup: []

must_haves:
  truths:
    - "I18nCore.t() 使用 ccb.* 命名空间键可返回对应语言的翻译"
    - "翻译键缺失时回退到英文，英文也缺失时返回键名本身"
    - "外部翻译文件（~/.ccb/i18n/ccb/{lang}.json）可覆盖内置翻译"
    - "伪翻译文件 xx.json 所有文本包含标记符"
    - "lib/i18n.py 改造后 t(key, **kwargs) 旧行为完全兼容"
    - "CCB_LANG 环境变量可控制语言选择"
  artifacts:
    - path: "lib/i18n_core.py"
      provides: "I18nCore 类，命名空间翻译框架"
      min_lines: 80
    - path: "lib/i18n/ccb/en.json"
      provides: "英文翻译，从 lib/i18n.py MESSAGES['en'] 迁移"
      contains: "ccb.error.no_terminal"
    - path: "lib/i18n/ccb/zh.json"
      provides: "中文翻译，从 lib/i18n.py MESSAGES['zh'] 迁移"
      contains: "ccb.error.no_terminal"
    - path: "lib/i18n/ccb/xx.json"
      provides: "伪翻译文件，测试布局溢出"
      contains: "no_terminal"
    - path: "tests/test_i18n_core.py"
      provides: "I18nCore 单元测试"
      min_lines: 60
    - path: "tests/demo_i18n_core.py"
      provides: "Demo 脚本，演示 i18n_core 功能"
  key_links:
    - from: "lib/i18n.py"
      to: "lib/i18n_core.py"
      via: "I18nCore(namespace='ccb').t()"
      pattern: "from lib.i18n_core import I18nCore"
    - from: "lib/i18n_core.py"
      to: "lib/i18n/ccb/{lang}.json"
      via: "Path(__file__).parent / 'i18n' / namespace"
      pattern: "i18n.*ccb.*\\.json"
---

<objective>
实现 i18n_core 最小原型并验证命名空间、t() API、回退机制和向后兼容性。

Purpose: 这是 PROTO-01 的核心交付，验证 Phase 2 设计的 i18n_core 架构可落地运行。
Output: I18nCore 类 + 3 个翻译 JSON 文件 + 兼容的 lib/i18n.py + 单元测试 + Demo 脚本
</objective>

<execution_context>
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/workflows/execute-plan.md
@E:/GitHub开源项目/TachiKuma/claude_code_bridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-原型验证/04-CONTEXT.md
@.planning/phases/02-架构设计/designs/i18n_core_design.md
@.planning/phases/02-架构设计/designs/translation_structure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建翻译文件和 I18nCore 类</name>
  <files>lib/i18n_core.py, lib/i18n/ccb/en.json, lib/i18n/ccb/zh.json, lib/i18n/ccb/xx.json</files>
  <read_first>
    - lib/i18n.py（读取全部 56 条消息的 MESSAGES 字典，需要完整迁移）
    - .planning/phases/02-架构设计/designs/i18n_core_design.md（I18nCore 类完整设计，包含日志、命名空间、回退、类型注解）
    - .planning/phases/02-架构设计/designs/translation_structure.md（翻译文件目录结构和 JSON 格式）
  </read_first>
  <action>
    **1. 创建目录 `lib/i18n/ccb/`**

    **2. 创建翻译文件 `lib/i18n/ccb/en.json`：**
    - 从 lib/i18n.py MESSAGES["en"] 提取全部 56 条消息
    - 为每个键添加 `"ccb."` 命名空间前缀，按类别分组：
      - Terminal detection: `ccb.terminal.*`
      - Startup: `ccb.startup.*`
      - Claude: `ccb.claude.*`
      - Banner: `ccb.banner.*`
      - Errors: `ccb.error.*`
      - Connectivity: `ccb.connectivity.*`
      - Commands: `ccb.command.*`
      - Install: `ccb.install.*`
    - 保持参数化占位符 `{provider}`, `{session}`, `{pane_id}` 等不变
    - JSON 格式，`ensure_ascii=False`, `indent=2`
    - 示例键映射（参考设计文档 Section 12）：
      - `"no_terminal_backend"` → `"ccb.terminal.no_terminal_backend"`
      - `"starting_backend"` → `"ccb.startup.starting_backend"`
      - `"sending_to"` → `"ccb.command.sending_to"`

    **3. 创建翻译文件 `lib/i18n/ccb/zh.json`：**
    - 同样从 MESSAGES["zh"] 提取 56 条消息
    - 使用与 en.json 完全相同的键名
    - 值为中文翻译

    **4. 创建伪翻译文件 `lib/i18n/ccb/xx.json`：**
    - 键名与 en.json 完全相同
    - 值格式：原文前后加 `[«»]` 标记，且字符串长度至少拉长到原文的 1.5 倍（不足部分用 `x` 填充）
    - 示例：`"No terminal backend detected"` → `"[«No terminal backend detectedxxxxxxxx»]"`
    - 示例（带占位符）：`"Starting {provider} backend ({terminal})..."` → `"[«Starting {provider} backend ({terminal})...xxxxxxxxxxxxxxxxxxx»]"`

    **5. 实现 `lib/i18n_core.py`（I18nCore 类）：**
    按照 i18n_core_design.md Section 10 和 Section 13（日志系统）的完整设计实现：

    ```python
    class I18nCore:
        def __init__(self, namespace: str = "ccb"):
            self.namespace = namespace
            self.translations: Dict[str, str] = {}
            self.current_lang: Optional[str] = None
            self.logger = logging.getLogger(f"i18n.{namespace}")

        def load_translations(self) -> None:
            # 1. _detect_language() 获取语言
            # 2. 加载内置翻译: Path(__file__).parent / "i18n" / self.namespace / f"{lang}.json"
            # 3. 加载外部翻译覆盖: Path.home() / ".ccb" / "i18n" / self.namespace / f"{lang}.json"
            # 4. 日志: INFO 级别记录检测结果和文件加载

        def _detect_language(self) -> str:
            # 优先级: CCB_LANG 环境变量 → 系统 locale → 默认 "en"
            # 支持: zh/cn/chinese → "zh", en/english → "en"

        def t(self, key: str, **kwargs) -> str:
            # 1. self.translations.get(key, key) — 键缺失返回键名
            # 2. str.format(**kwargs) 参数化，失败时返回未格式化消息
            # 3. 键缺失时 logger.warning()

        def _load_json_file(self, path: Path) -> Dict[str, str]:
            # 加载 JSON 翻译文件，错误处理（JSONDecodeError → logger.error）
            # 文件不存在 → 返回空字典，不报错
    ```

    不引入任何新依赖，仅使用 Python 标准库（json, os, locale, logging, pathlib）。
  </action>
  <verify>
    <automated>python -c "from lib.i18n_core import I18nCore; i = I18nCore('ccb'); i.load_translations(); print(i.t('ccb.terminal.no_terminal_backend'))"</automated>
  </verify>
  <done>
    - lib/i18n/ccb/en.json 存在且包含 `"ccb.terminal.no_terminal_backend"` 键
    - lib/i18n/ccb/zh.json 存在且包含 `"ccb.terminal.no_terminal_backend"` 键
    - lib/i18n/ccb/xx.json 存在且值包含 `[«»]` 标记
    - lib/i18n_core.py 存在且包含 `class I18nCore`
    - `I18nCore("ccb").load_translations()` 可执行不报错
    - `I18nCore("ccb").t("ccb.terminal.no_terminal_backend")` 返回翻译值
  </done>
</task>

<task type="auto">
  <name>Task 2: 单元测试 + 兼容层改造 + Demo 脚本</name>
  <files>tests/test_i18n_core.py, tests/demo_i18n_core.py, lib/i18n.py</files>
  <read_first>
    - lib/i18n.py（当前实现，需要改造为调用 i18n_core）
    - lib/i18n_core.py（Task 1 刚创建的文件）
    - lib/i18n/ccb/en.json（翻译文件，确认键名格式）
  </read_first>
  <action>
    **1. 改造 `lib/i18n.py`（向后兼容层）：**

    保持现有全部公共 API 不变：`detect_language()`, `get_lang()`, `set_lang()`, `t(key, **kwargs)`。
    内部改为使用 I18nCore：

    ```python
    from lib.i18n_core import I18nCore

    _core = I18nCore("ccb")
    _core_loaded = False

    def _ensure_loaded():
        global _core_loaded
        if not _core_loaded:
            _core.load_translations()
            _core_loaded = True

    def t(key: str, **kwargs) -> str:
        _ensure_loaded()
        # 兼容旧键名：将旧键映射到新的命名空间键
        namespaced_key = _key_mapping.get(key, key)
        return _core.t(namespaced_key, **kwargs)
    ```

    创建 `_key_mapping` 字典，将旧的 56 个键名映射到新的 `ccb.*` 命名空间键：
    ```python
    _key_mapping = {
        "no_terminal_backend": "ccb.terminal.no_terminal_backend",
        "solutions": "ccb.terminal.solutions",
        "install_wezterm": "ccb.terminal.install_wezterm",
        # ... 全部 56 个键
    }
    ```

    `detect_language()`、`get_lang()`、`set_lang()` 保持原有实现不变。
    不删除 MESSAGES 字典（保持向后兼容），但 t() 函数实际走 i18n_core 路径。

    **2. 创建 `tests/test_i18n_core.py`：**

    使用 Python 内置 unittest 框架（per D-17），测试用例：

    ```python
    import unittest
    import os
    import json
    import tempfile
    from pathlib import Path
    from lib.i18n_core import I18nCore

    class TestI18nCore(unittest.TestCase):
        def test_load_builtin_translations(self):
            """内置翻译文件加载成功"""
            i = I18nCore("ccb")
            i.load_translations()
            self.assertGreater(len(i.translations), 50)

        def test_namespace_key_lookup(self):
            """命名空间键可查找"""
            i = I18nCore("ccb")
            i.load_translations()
            result = i.t("ccb.terminal.no_terminal_backend")
            self.assertNotEqual(result, "ccb.terminal.no_terminal_backend")

        def test_missing_key_returns_key(self):
            """缺失键返回键名本身"""
            i = I18nCore("ccb")
            i.load_translations()
            result = i.t("ccb.nonexistent.key")
            self.assertEqual(result, "ccb.nonexistent.key")

        def test_format_parameters(self):
            """参数化消息格式化"""
            i = I18nCore("ccb")
            i.load_translations()
            result = i.t("ccb.startup.started_backend", provider="test", terminal="wezterm", pane_id="5")
            self.assertIn("test", result)

        def test_language_detection_env_var(self):
            """CCB_LANG 环境变量控制语言"""
            os.environ["CCB_LANG"] = "zh"
            i = I18nCore("ccb")
            i.load_translations()
            self.assertEqual(i.current_lang, "zh")
            # 恢复
            del os.environ["CCB_LANG"]

        def test_fallback_to_english(self):
            """非英文语言缺失键回退到英文"""
            # 添加一个只有英文的键
            os.environ["CCB_LANG"] = "zh"
            i = I18nCore("ccb")
            i.load_translations()
            # 在翻译中临时添加一个仅英文存在的键
            i.translations["test.only.en"] = "English only"
            result = i.t("test.only.en")
            self.assertEqual(result, "English only")
            del os.environ["CCB_LANG"]

        def test_external_translation_override(self):
            """外部翻译文件覆盖内置翻译"""
            with tempfile.TemporaryDirectory() as tmpdir:
                ext_dir = Path(tmpdir) / ".ccb" / "i18n" / "ccb"
                ext_dir.mkdir(parents=True)
                (ext_dir / "en.json").write_text(
                    json.dumps({"ccb.startup.started_backend": "CUSTOM OVERRIDE"}),
                    encoding="utf-8"
                )
                # Monkey-patch 外部路径（因为实际是 Path.home()）
                original_home = Path.home
                try:
                    import pathlib
                    pathlib.Path.home = lambda: Path(tmpdir)
                    # 创建内置翻译的临时结构
                    i = I18nCore("ccb")
                    i.load_translations()
                    result = i.t("ccb.startup.started_backend")
                    self.assertEqual(result, "CUSTOM OVERRIDE")
                finally:
                    pathlib.Path.home = original_home

        def test_pseudo_translation_has_markers(self):
            """伪翻译文件包含 [«»] 标记"""
            os.environ["CCB_LANG"] = "xx"
            i = I18nCore("ccb")
            i.load_translations()
            result = i.t("ccb.terminal.no_terminal_backend")
            self.assertIn("[«", result)
            self.assertIn("»]", result)
            del os.environ["CCB_LANG"]
    ```

    **3. 创建 `tests/demo_i18n_core.py`（Demo 脚本）：**

    ```python
    #!/usr/bin/env python3
    """i18n_core Demo - 演示命名空间翻译、回退机制和外部覆盖"""
    import os
    import sys
    sys.path.insert(0, ".")
    from lib.i18n_core import I18nCore

    print("=== i18n_core Demo ===\n")

    # 1. 英文
    print("--- English (default) ---")
    os.environ.pop("CCB_LANG", None)
    i = I18nCore("ccb")
    i.load_translations()
    print(f"Language: {i.current_lang}")
    print(f"  {i.t('ccb.terminal.no_terminal_backend')}")
    print(f"  {i.t('ccb.startup.started_backend', provider='Claude', terminal='tmux', pane_id='3')}")
    print()

    # 2. 中文
    print("--- Chinese (CCB_LANG=zh) ---")
    os.environ["CCB_LANG"] = "zh"
    i2 = I18nCore("ccb")
    i2.load_translations()
    print(f"Language: {i2.current_lang}")
    print(f"  {i2.t('ccb.terminal.no_terminal_backend')}")
    print()

    # 3. 伪翻译
    print("--- Pseudo (CCB_LANG=xx) ---")
    os.environ["CCB_LANG"] = "xx"
    i3 = I18nCore("ccb")
    i3.load_translations()
    print(f"Language: {i3.current_lang}")
    print(f"  {i3.t('ccb.terminal.no_terminal_backend')}")
    print()

    # 4. 回退机制
    print("--- Fallback (missing key) ---")
    os.environ.pop("CCB_LANG", None)
    i4 = I18nCore("ccb")
    i4.load_translations()
    print(f"  Missing key: {i4.t('ccb.nonexistent.key')}")
    print()

    print("=== Demo complete ===")
    ```
  </action>
  <verify>
    <automated>cd "E:/GitHub开源项目/TachiKuma/claude_code_bridge" && python -m pytest tests/test_i18n_core.py -v 2>&1 | head -30</automated>
  </verify>
  <done>
    - tests/test_i18n_core.py 包含至少 6 个测试用例
    - `python -m pytest tests/test_i18n_core.py` 全部通过
    - lib/i18n.py 包含 `from lib.i18n_core import I18nCore`
    - lib/i18n.py 包含 `_key_mapping` 字典
    - lib/i18n.py 的 `t()` 函数调用 `_core.t()`
    - tests/demo_i18n_core.py 可执行（`python tests/demo_i18n_core.py`）且输出 4 种场景
    - 旧的 `t("no_terminal_backend")` 调用仍然返回正确翻译值（向后兼容）
  </done>
</task>

</tasks>

<verification>
1. `python tests/demo_i18n_core.py` 输出英文、中文、伪翻译和回退 4 种场景
2. `python -m pytest tests/test_i18n_core.py` 全部通过
3. `python -c "from lib.i18n import t; print(t('no_terminal_backend'))"` 返回旧格式的翻译值
4. `CCB_LANG=zh python -c "from lib.i18n import t; print(t('no_terminal_backend'))"` 返回中文翻译
</verification>

<success_criteria>
- I18nCore 类可通过命名空间键加载翻译，支持 3 种语言
- t(key, **kwargs) API 完整可用，支持参数化格式化
- 回退链（当前语言→英文→键名）工作正常
- lib/i18n.py 向后兼容：旧键名调用返回正确值
- 单元测试至少 6 个用例，全部通过
- Demo 脚本展示英文、中文、伪翻译、回退 4 种场景
</success_criteria>

<output>
After completion, create `.planning/phases/04-原型验证/04-01-SUMMARY.md`
</output>
