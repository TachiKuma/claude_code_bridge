<!-- CCB_CONFIG_START -->
## AI 协作

完整的 CCB 多 agent 配置见 `~/.claude/rules/ccb-config.md`。
常用命令：`/ask <provider>` 用于咨询，`/cping <provider>` 用于检查连通性，`/pend <provider>` 用于查看回复。
可用 provider：`codex`、`gemini`、`opencode`、`droid`、`claude`
<!-- CCB_CONFIG_END -->
