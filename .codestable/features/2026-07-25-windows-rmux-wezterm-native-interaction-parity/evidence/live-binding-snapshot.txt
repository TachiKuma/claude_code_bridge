Command: python -m pytest -q -rs test/test_v2_tmux_ui.py -k rmux_accepts_mouse_context_project_ui_bindings
Result: passed
Output:
.                                                                        [100%]
1 passed, 14 deselected in 0.51s

Live list-keys snapshot:
returncode=0
bind-key -T root MouseDown1Pane            if-shell -F -t = "#{==:#{@ccb_role},sidebar}" "select-pane -t = ; send-keys -t = -M" "select-pane -t ="
bind-key -T root MouseDown1Border          if-shell -F -t = "#{==:#{@ccb_role},sidebar}" "select-pane -t = ; send-keys -t = -M" "select-pane -t ="
bind-key -T root WheelDownPane             if-shell -F -t = "#{==:#{@ccb_role},sidebar}" "select-pane -t = ; send-keys -t = -M" "select-pane -t ="
bind-key -T root WheelUpPane               if-shell -F -t = "#{==:#{@ccb_role},sidebar}" "select-pane -t = ; send-keys -t = -M" "select-pane -t ="

Evidence:
- rmux accepted root mouse bindings generated by _apply_sidebar_mouse_controls() under Windows/rmux fallback.
- Scoped root bindings include MouseDown1Pane, MouseDown1Border, WheelUpPane, and WheelDownPane.
- Sidebar panes always receive the original mouse event via `select-pane -t = ; send-keys -t = -M`; header settings / x handling is delegated to Rust `header_action_at`.
- Ordinary panes receive only `select-pane -t =` on these fallback mouse events; they do not enter copy-mode, paste-buffer, or scroll commands.
- Scoped root bindings do not include MouseDrag1Pane, MouseDown3Pane, or M-MouseDown3Pane.
- Scoped root bindings do not contain #{mouse_pane}, #{mouse_x}, #{mouse_y}, select-pane -M, copy-mode -e, history_size, alternate_on, scroll-up, scroll-down, paste-buffer, send-keys -t = c, or send-keys -t = Q.
- Each scoped binding evaluates the sidebar condition against the mouse pane via `if-shell -F -t =`.
