---
doc_type: goal-iteration
goal: "ccbd-windows-full-chain-smoke"
iteration: 3
status_after: active
next_action: "Run independent Task agent functional acceptance against the recorded PS5/PS7 transcripts and current diff before marking the goal complete."
blocker_signature: null
functional_acceptance: null
updated_at: "2026-07-23"
---

# Iteration 003

## Current Understanding

owner 已确认 `expand-scope-windows-runtime-fix` 与 `restore-rmux-090-dependency`。本轮不再把 CMD-004 阻塞当作 smoke 资产问题处理，而是按已批准范围修复 Windows rmux runtime 的真实链路问题，并要求 PowerShell 5 与 PowerShell 7 都跑同一真机脚本。

## Implementation Approach

本轮保持 fail-closed：

- 不用 WSL、probe、direct rmux 或 fake backend 冒充 full-chain pass。
- parser 继续要求 native Windows、ccbd control plane、rmux backend、tcp_loopback transport、runtime/session evidence 和 cleanup residue evidence。
- fake provider 只在 `CCB_TEST_ENTRYPOINT=1` 入口下用于验证 ccbd ask 链路，不替代 backend/runtime evidence。
- 生产 runtime 改动只覆盖 owner 已批准的 Windows runtime fix 与 rmux 0.9.0 兼容问题。

## Changes This Iteration

- project namespace runtime：
  - 对 rmux pane mutation 统一包装 `MuxPaneRef`，覆盖 split、respawn、user option、pane identity。
  - Windows split pane 使用完整 placeholder shell command，避免 child pane 立即退出。
- rmux 0.9.0 兼容：
  - `respawn-pane` 不再发送 rmux 0.9.0 不支持的 `-P`。
- Windows sidebar respawn：
  - Windows 使用 PowerShell `$env:CCB_SIDEBAR_THEME_PROFILE=...; & ...` 形式，POSIX 保持原 env prefix。
- runtime authority evidence：
  - `namespace_backend_impl` 贯穿 start flow、supervisor lifecycle 和 reload mount start。
  - namespace assigned rmux pane 写入 `runtime_ref=rmux:%pane`、`terminal_backend=rmux`、`backend_impl=rmux`、`pane_ref`。
  - warm reuse restored 且无 namespace 接管时保留既有 `workspace_path` authority，避免 Windows path 字符串造成无意义 store write。
- smoke runner/parser：
  - parser 接受 `ccb ps` 的 `binding: runtime=... pane=...` 作为带标签 runtime identity。
  - parser 要求顶层 `backend_selection_source` 与 `evidence.backend_selection.source` 一致。
  - parser 要求 transcript 包含 `runner_host`，记录 PowerShell edition/version/executable。
  - transcript redaction scan 覆盖 JSON escaped Windows home path，token regex 避免把 `ask-evidence` 误判为 secret。
  - transport evidence 从 ping/doctor artifact 合并识别。
  - cleanup endpoint residue 检查真实 endpoint/token 文件，不要求删除 `.ccb/ccbd` 状态目录。
  - scope guard 允许本轮 owner 已批准的 expanded runtime/backend 修复文件，仍拒绝未授权 docs/packaging/provider parser/相邻 tmux runtime 路径。
- CodeStable 记录：
  - 写入 `.codestable/issues/2026-07-23-windows-rmux-smoke-runtime-fixes/windows-rmux-smoke-runtime-fixes-fix-note.md`。
  - 更新 full-chain smoke checklist 中 CMD-004/CMD-005 和 start/ping/ask/cleanup 证据状态。

## Verification Evidence

- Focused warm reuse regression：
  - `python -m pytest -q test/test_ccbd_start_agent_runtime.py::test_real_runtime_service_warm_reuse_preserves_restored_without_store_write` -> passed。
  - `python -m pytest -q test/test_ccbd_start_agent_runtime.py::test_start_agent_runtime_records_rmux_namespace_pane_binding` -> passed。
- Parser/scope：
  - `python -m pytest -q test/test_ccbd_windows_full_chain_smoke.py` -> `30 passed`。
  - `python scripts/ccbd_windows_full_chain_smoke.py --scope-guard --diff-base HEAD --json` -> `ok: true`，`forbidden_paths=[]`。
- Related regression：
  - `python -m pytest -q test/test_rmux_backend_core.py test/test_terminal_runtime_rmux.py test/test_provider_helper_cleanup.py test/test_cli_kill_runtime_processes.py test/test_ccbd_stop_flow_runtime.py test/test_ccbd_windows_full_chain_smoke.py test/test_ccbd_start_agent_runtime.py test/test_ccbd_sidebar_helper.py test/test_ccbd_namespace_additive_patch.py test/test_v2_project_namespace_state.py` -> `172 passed`。
- CMD-004 / Windows PowerShell 5：
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File ./scripts/ccbd-windows-full-chain-smoke.ps1 -ProjectRoot $env:TEMP/ccb-rmux-full-chain-ps5-v14 -Backend rmux -AskCaseKind fake_provider -Json` -> parser `ok: true`。
  - PS5 pass artifact: `artifacts/ccbd-windows-full-chain-smoke-ps5-pass-20260723-142213/transcript.json`，`runner_host.edition=Desktop`，`runner_host.version=5.1.19041.6157`。
- CMD-005 / PS5 transcript parser：
  - `python scripts/ccbd_windows_full_chain_smoke.py --transcript artifacts/ccbd-windows-full-chain-smoke-ps5-pass-20260723-142213/transcript.json --json` -> parser `ok: true`。
- CMD-004 / PowerShell 7：
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/ccbd-windows-full-chain-smoke.ps1 -ProjectRoot $env:TEMP/ccb-rmux-full-chain-pwsh7-v2 -Backend rmux -AskCaseKind fake_provider -Json` -> parser `ok: true`。
  - PS7 current artifact: `artifacts/ccbd-windows-full-chain-smoke/transcript.json`，`runner_host.edition=Core`，`runner_host.version=7.5.4`。
- CMD-005 / PS7 transcript parser：
  - `python scripts/ccbd_windows_full_chain_smoke.py --transcript artifacts/ccbd-windows-full-chain-smoke/transcript.json --json` -> parser `ok: true`。

## Problems Encountered

- 真实 PS5 run 最初通过 product transcript 但 parser 失败，原因是 smoke 脚本 evidence 设计未对齐当前 CLI 输出：
  - `transport.kind` 只看 doctor，但 `tcp_loopback` 在 ping artifact。
  - runtime identity 在 `ccb ps` 中是 `binding: runtime=rmux:%1`，不是单独 `runtime_ref:` 行。
  - cleanup 把保留状态目录 `.ccb/ccbd` 当作 endpoint residue。
- 这些问题均已用 parser/runner 修改和单测覆盖修复。
- 独立 Task agent 初次功能验收发现两个 fail-closed 缺口：backend selection source 内部矛盾未失败、transcript escaped home path 未被 redaction scan 捕获。本轮已补 parser 与 runner，并新增回归用例后重跑 PS5/PS7。

## Next Attempt

启动独立 Task agent 做功能验收：检查 owner acceptance、当前 diff、PS5/PS7 transcript 和 parser evidence，确认没有 fake backend/probe/direct rmux 旁路，且 cleanup/runtime evidence 满足 full-chain done signal。

## State Update

Goal 保持 `active`，`current_iteration` 更新到 3。代码和真机验证已通过，但根据 `cs-goal` 规则，未写 `functional-acceptance.md` 且未由独立 Task agent pass 前不得标记 complete。
