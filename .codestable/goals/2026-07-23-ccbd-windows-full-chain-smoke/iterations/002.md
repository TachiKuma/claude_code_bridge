---
doc_type: goal-iteration
goal: "ccbd-windows-full-chain-smoke"
iteration: 2
status_after: blocked
next_action: "Wait for owner decision on whether to expand scope to fix Windows runtime cleanup/SIGKILL and restore or install a matching rmux 0.9.0 binary before rerunning CMD-004."
blocker_signature: "native-windows-rmux-runtime-prerequisites-not-satisfied"
functional_acceptance: null
updated_at: "2026-07-23"
---

# Iteration 002

## Current Understanding

owner 已批准执行 CMD-004。runner/parser 资产本身仍然通过 focused tests 和 scope guard，但真实 native Windows full-chain 没有通过，原因不是 transcript parser 误报，而是暴露了 goal 边界外的 runtime/dependency 前提问题。

## Implementation Approach

本轮保持验证资产与生产 runtime 分离：

- 优先尝试 installed `ccb`，确认它不是当前项目的 `--project` CLI，不能用于本 smoke。
- 使用 repo-local `python ccb.py`，并按 source guard 文案与 owner 已批准的 CMD-004 风险，把 `CCB_SOURCE_RUNTIME_OK=1` 作为显式 diagnostics override 记录到 command allowlist。
- 在临时 project 中复制已有 rmux route approval 与 decision summary，让 `--project` 指向 temp 时也能读取 route decision。
- 将默认 ask target 从不存在的 `ccb_self` 改为临时项目实际启动的 `demo`。
- 调整 parser 的 transport artifact marker：接受 `ccb ping ccbd` 或 `ccb doctor` 中的 `tcp_loopback` 证据，因为当前 CLI 在 ping 输出中打印 control-plane endpoint。

## Changes This Iteration

- 更新 `scripts/ccbd-windows-full-chain-smoke.ps1`：
  - 默认 `AskTarget=demo`。
  - source diagnostics 模式显式设置 `CCB_SOURCE_RUNTIME_OK=1`。
  - 临时 project bootstrap rmux route approval fixture。
- 更新 `scripts/ccbd_windows_full_chain_smoke.py`：
  - artifact scan 仍 fail closed，但 transport marker 从 doctor-only 改为 ping-or-doctor。
- 更新 `test/test_ccbd_windows_full_chain_smoke.py`：
  - 同步 transport marker 的错误断言。

## Verification Evidence

- `python -m pytest -q "test/test_ccbd_windows_full_chain_smoke.py"` -> 25 passed。
- PowerShell 静态解析 `scripts/ccbd-windows-full-chain-smoke.ps1` -> `parse-ok`。
- `python "scripts/ccbd_windows_full_chain_smoke.py" --scope-guard --diff-base HEAD --json` -> `ok: true`，无 forbidden paths。
- CMD-004 第一次 installed `ccb` 尝试失败：installed `ccb.bat --help` 可运行，但 `ccb --project ...` 返回 `unrecognized arguments: --project`。
- CMD-004 source guard override 后，`ccb start` / `ccb ping ccbd` / `ccb doctor` 证明 ccbd mounted、TCP loopback endpoint 可用；route approval bootstrap 后 doctor 记录 `backend_selection_backend_impl: rmux`、`backend_selection_effective: rmux`。
- 最新真实 transcript：`artifacts/ccbd-windows-full-chain-smoke/transcript.json`，parser verdict 仍为 `test_design_failure`，核心错误为 ask artifact 为空。

## Problems Encountered

真实 runtime 失败点如下：

- 当前 PATH 上的 `rmux` 是 `rmux 0.8.0`：`artifacts/ccbd-windows-full-chain-smoke/commands/preflight-rmux-version.stdout.txt`。
- 历史 route/capability approval 选择的可用证据是 `rmux 0.9.0`，但当前机器上先前记录的 WinGet 0.9.0 路径已不存在；`Get-Command rmux -All` 只发现 `C:\Users\Administrator\AppData\Local\rmux\bin\rmux.exe`。
- `ccb start` / `ccb ask` / `ccb kill -f` 在 Windows 上触发生产 runtime 异常：`module 'signal' has no attribute 'SIGKILL'`。
- `ccb kill -f` 也因同一 `SIGKILL` bug 失败，runner 的 cleanup evidence 因此保持 failed，未能证明 endpoint/token/runtime residue 清理。
- `ccb ask demo --artifact-reply ...` 有 job event 记录，但因 project shutdown / runtime failure，artifact reply 内容是 `Project stopped before this request completed.`，不满足 full-chain pass。

## Next Attempt

需要 owner 决定是否扩大范围：

- 修复生产 runtime Windows `signal.SIGKILL` cleanup/startup 路径，并创建对应 issue/fix 产物；这会越过本 goal 当前 scope guard。
- 恢复或安装与 route approval 匹配的 `rmux 0.9.0`，并通过 `CCB_RMUX_BIN` 指向该 binary；这涉及本机依赖状态变更。
- 保持 blocked，不把当前 transcript 伪装成 pass。

## State Update

Goal 进入 owner-stop：`ScopeBoundaryChange`。当前状态为 `blocked`，不是完成；最新可恢复状态见 `state.yaml`，新的 owner 决策面见 `approval-report.md`。
