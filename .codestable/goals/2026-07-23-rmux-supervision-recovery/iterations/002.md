---
doc_type: goal-iteration
goal: "rmux-supervision-recovery"
iteration: 2
status_after: complete
next_action: "Goal complete; downstream true-host destructive recovery smoke remains in rmux-windows-validation-matrix."
blocker_signature: null
functional_acceptance: "../functional-acceptance.md"
updated_at: "2026-07-23"
---

# Iteration 002

## Current Understanding

Iteration 001 已完成 evidence ledger、backend-neutral pane/namespace matching、process/job recovery split 和 namespace crash reflow。本轮剩余风险集中在 daemon ownership diagnostics/recovery、diagnostics projection、scope guard 和独立 Task agent gates。

## Implementation Approach

保持 supervision 为 recovery owner，按 SOLID / KISS 收束在现有 state machine、runtime facts 和 diagnostics projection 上：

- 不改 provider parser、backend resolver、namespace lifecycle 或 process liveness implementation。
- 不为 shared daemon 引入 kill/restart 分支。
- 只在现有 recovery event/details 和 diagnostics bundle summary 中补足 evidence/action。

## Changes This Iteration

- Runtime authority 增加 `daemon_ref` 持久化和 attach/refresh/provider facts 传递。
- Recovery policy 增加 process、namespace、daemon health sets；daemon recovery 只允许 `project` / `owned` scope。
- Supervision 对 shared / unowned daemon crash 写 `daemon_degraded` event，包含 `action=degraded_only`、`ownership=shared_or_unowned` 和 evidence ledger；不调用 refresh，不增加 `restart_count`。
- Owned/project daemon generation mismatch 保持 recovery path，并记录 `daemon_recover`。
- Project view、ping、doctor、diagnostics bundle 展示 evidence ledger；bundle generated `supervision-ledger.json` 直接包含 action/reason/ownership。
- 新增/扩展 Rmux daemon supervision、diagnostics projection、scope guard 和 v2 regression 测试。
- 写回 feature review、QA、acceptance、roadmap items 和 goal state。

## Verification Evidence

- `python ".codestable/tools/validate-yaml.py" --file ".codestable/features/2026-07-20-rmux-supervision-recovery/rmux-supervision-recovery-checklist.yaml" --yaml-only`：passed。
- `python ".codestable/tools/validate-yaml.py" --file ".codestable/roadmap/windows-rmux-native-backend/windows-rmux-native-backend-items.yaml"`：passed。
- `python -m pytest -q "test/test_ccbd_supervision.py" "test/test_ccbd_supervision_recovery.py" "test/test_ccbd_rmux_supervision_evidence.py" "test/test_ccbd_rmux_supervision_recovery.py" "test/test_ccbd_rmux_daemon_supervision.py" "test/test_ccbd_supervision_diagnostics.py" "test/test_ccbd_project_view.py" "test/test_cli_doctor_supervision.py" "test/test_cli_ping_supervision.py" "test/test_ccbd_diagnostics_bundle_supervision.py" "test/test_ccbd_rmux_supervision_guard.py"`：`137 passed`。
- `python -m pytest -q "test/test_v2_ccbd_supervision_loop.py" "test/test_v2_ccbd_ping_runtime.py" "test/test_v2_diagnostics_bundle.py" "test/test_v2_tmux_cleanup_history.py"`：`63 passed`。
- 独立二次 code review Task agent：`codex-gpt5-readonly-secondary` verdict `pass`。
- 独立二次 functional acceptance Task agent：`task-agent-rmux-supervision-recovery-fa2-20260723` verdict `pass`。
- 窄范围 diagnostics ledger Task agent：`codex-functional-acceptance-diagnostics-ledger` verdict `pass`。

## Problems Encountered

初次 review / acceptance 均发现 shared daemon crash 只阻止 recovery、但未写 degraded event，导致 diagnostics bundle 无法直接反查 recovery action。修复后又补足 generated `supervision-ledger.json` 的 action/reason/ownership 字段，消除只读 raw event 才能看见 `degraded_only` 的残余风险。

## Next Attempt

本 goal 已完成。真实 Windows/Rmux destructive kill smoke 不在当前项目上执行，后续由 `rmux-windows-validation-matrix` 在 disposable true-host 场景补足。

## State Update

`state.yaml.status` 更新为 `complete`，`current_iteration` 更新为 `2`，功能验收见 `../functional-acceptance.md`。
