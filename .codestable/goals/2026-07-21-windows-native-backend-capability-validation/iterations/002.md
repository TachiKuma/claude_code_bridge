---
doc_type: goal-iteration
goal: "windows-native-backend-capability-validation"
iteration: 2
status_after: blocked
next_action: "等待 owner 批准是否临时执行 psmux 3.3.7；若不批准，则基于当前安装状态进入验收并记录已安装候选不达标。"
blocker_signature: "psmux-latest-install-risk-acceptance"
functional_acceptance: null
updated_at: "2026-07-21"
---

# Iteration 002

## Current Understanding

上一轮已经触发 `RiskAcceptanceNeeded` owner-stop：验证 `psmux 3.3.7` 需要下载并执行新的外部二进制，或通过 Winget 改变本机安装状态。当前用户没有给出明确“确认/继续/是 + 选项”，因此不能执行升级或临时二进制。

## Implementation Approach

继续做不改变系统状态的证据补强：收集 GitHub release metadata、Winget package metadata、本机安装 inventory，并把当前 `rmux 0.9.0` / `psmux 3.3.3` 的候选结论整理成矩阵。

## Changes This Iteration

- 新增 `evidence/psmux-337-release-metadata.json`，记录 GitHub latest release `v3.3.7` 和 Windows zip / installer 资产。
- 新增 `evidence/winget-psmux-337-show.txt`，记录 Winget `marlocarlo.psmux 3.3.7`、x64 portable zip URL 和 SHA256。
- 新增 `evidence/winget-rmux-show.txt`，记录 Winget `Helvesec.RMUX 0.9.0` 与 installer SHA256。
- 新增 `evidence/local-backend-inventory.json`，记录本机 `rmux` / `psmux` 解析路径、版本、目录内容和当前无 probe 残留进程。
- 新增 `backend-validation-conclusion.md`，按 `ccb` Windows 需求列出候选 backend 的验证矩阵和当前 verdict。
- 修正 `approval-report.md` 中选项推荐不一致的问题：推荐 C，即临时下载到 evidence 目录并用显式路径验证。

## Verification Evidence

- GitHub release API：`psmux` latest = `v3.3.7`，发布时间 `2026-07-20T07:56:43Z`。
- `winget show --id marlocarlo.psmux --exact`：版本 `3.3.7`，installer type = portable zip，x64 SHA256 = `60ff7b236f64184921cef3c1ff2611aa5a36fcc7ed8e2a58e968b8ded57f6028`。
- `winget show --id Helvesec.RMUX --exact`：版本 `0.9.0`，说明为 daemon-backed persistent sessions / native Windows support。
- `local-backend-inventory.json`：PATH 上 `psmux` 仍为 `C:\Users\Administrator\AppData\Local\psmux\psmux.exe`，版本 `psmux 3.3.3`；`rmux` 为 Winget `0.9.0`；无 `rmux` / `psmux` probe 残留进程。

## Problems Encountered

`psmux 3.3.7` 的执行验证仍需要 owner 明确授权。没有该授权时，不能完成“最新版 psmux capability gate”这条验收；但可以明确当前安装状态下的结论：已安装候选 backend 尚不能胜任完整 `ccb` Windows native backend 基座。

## Next Attempt

等待 owner 对 `approval-report.md#install-psmux-337-for-gate` 做选择：

- 选择 C：临时下载 `psmux 3.3.7` x64 zip 到 evidence 目录，校验 SHA256，用显式路径重跑 gate。
- 选择 B：不验证最新版，进入 Task agent 验收当前结论，verdict 预计为“当前安装候选不达标，最新版未验证”。

## State Update

`state.yaml.current_iteration` 更新为 2，保留 `status: blocked` 和 pending owner-stop；`blocker_count` 更新为 2。
