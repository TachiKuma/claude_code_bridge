schema_version: 2
goal: "mux-backend-contract"
status: complete
objective: "继续 windows-rmux-native-backend epic 中的 mux-backend-contract，交付 backend-neutral MuxBackend 契约、能力拆分、fake backend 测试替身、泄漏点 inventory，并通过 feature checklist 的核心验证。"
start_point: "backend-resolver-opt-in-contract 已验收通过；mux-backend-contract design 已 approved，checklist 仍为 pending；epic goal-state 在 GoalDriverUnavailable handoff 后要求从 mux-backend-contract 继续。"
acceptance:
  - "新增 contract module 定义 MuxNamespaceRef、MuxPaneRef、MuxCapabilities、MuxCommandError，且不依赖 tmux implementation module。"
  - "MuxBackend 使用 namespace/window/pane io/presentation/logging/diagnostics 小协议组合，不把 _tmux_run(args) 暴露为 public protocol。"
  - "fake backend 以 namespace/window/pane 状态机和 event log 支持 lifecycle、layout、io、presentation、logging、capabilities 与 failure injection。"
  - "记录当前 _tmux_run / tmux argv 泄漏点，作为 tmux-backend-contract-adapter 输入。"
  - "运行 mux-backend-contract checklist 中的核心验证命令，并更新 feature/epic 状态产物。"
non_goals:
  - "不实现 RmuxBackend 或调用 Rmux CLI/SDK。"
  - "不迁移 provider session payload。"
  - "不修改 ccbd control-plane transport endpoint schema。"
  - "不删除 TerminalBackend/TmuxBackend 现有 public methods。"
  - "不执行 git commit、git push、merge、release、deploy 或生产变更。"
budget:
  kind: none
  limit: null
current_iteration: 4
next_action: "Goal complete; continue the windows-rmux-native-backend epic with the next dependent feature."
blocker_signature: null
blocker_count: 0
owner_stop:
  status: resolved
  reason: ScopeBoundaryChange
  decision: "ResumeWith: owner stated backend-resolver-opt-in-contract was accepted and requested continuing mux-backend-contract."
updated_at: "2026-07-22"
