---
doc_type: goal-iteration
goal: "mux-backend-contract"
iteration: 3
status_after: active
next_action: "Run terminal functional acceptance with a visible Task agent, write functional-acceptance.md, then record the final iteration and complete state."
blocker_signature: null
functional_acceptance: null
updated_at: "2026-07-22"
---

# Iteration 003

## Current Understanding

独立 reviewer 对 `mux-backend-contract` diff 和 psmux 兼容修复做了只读 review，初次结论为 `verdict: pass`，但提出两条 important 风险：兼容层判断误扫 socket/config 参数，以及 optional tmux policy 丢失 transient retry。

## Implementation Approach

两条风险都应在 acceptance 前关闭。修复保持 KISS：兼容判断只看 backend implementation 或 executable 路径；optional policy 恢复既有 `_tmux_run_ready` retry，只保留新引入的 bool 返回语义。

## Changes This Iteration

- `is_tmux_compat_subset` 不再扫描 `_tmux_base()` 的全部参数，避免普通 tmux socket/config 路径包含 `psmux` 或 `rmux` 时被误判。
- `_apply_optional_tmux_policy` 恢复使用 `_tmux_run_ready(..., timeout_s=timeout_s)`，避免普通 tmux transient server unavailable 时静默漏应用可选策略。
- 增加负例测试：socket path 含 `psmux` 但 executable 是普通 tmux 时不能被判为 compat subset。
- 增加 transient retry 测试：`set-window-option -g mode-keys vi` fail-once 后应重试成功，并继续 keybinding policy。

## Verification Evidence

- Focused closure reviewer：`019f89ab-deb0-7861-adbd-be813d3a07b4`，结果 `verdict: pass`，已关闭。
- reviewer 只读验证：`python -m pytest -q test/test_v2_project_namespace_backend.py::test_ensure_server_policy_retries_transient_optional_window_policy test/test_v2_project_namespace_backend.py::test_tmux_compat_subset_ignores_socket_paths_containing_psmux`：2 passed。
- 本地主线验证：
  - `python -m pytest -q "test/test_v2_project_namespace_backend.py"`：19 passed。
  - `python -m pytest -q "test/test_v2_tmux_ui.py::test_apply_project_tmux_ui_skips_project_ui_for_psmux_compat_tmux_path"`：1 passed。
  - `python -m pytest -q "test/test_backend_selection_diagnostics.py" "test/test_agent_window_reflow.py" "test/test_v2_phase2_entrypoint.py" -k "tmux or layout or runtime"`：7 passed, 82 deselected。
  - `python -m pytest -q "test/test_mux_backend_contract.py"`：8 passed。
  - `python -m pytest -q "test/test_ccbd_windows_tcp_loopback_transport.py"`：34 passed。

## Problems Encountered

无新的稳定 blocker。

## Next Attempt

启动终端功能验收 Task agent，按 owner acceptance criteria 检查 contract/fake backend、泄漏点 inventory、feature checklist 和验证证据。验收通过后写 `functional-acceptance.md` 与 final iteration，并将 `state.yaml` 标为 `complete`。

## State Update

`state.yaml` 更新为 `current_iteration: 3`，goal 保持 `active`。剩余 gate 是终端 functional acceptance。
