---
doc_type: goal-iteration
goal: "windows-rmux-capability-gate"
iteration: 2
status_after: complete
next_action: "Capability gate complete but not passed; 后续需先解决 psmux required gaps 或更换 backend 基座。"
blocker_signature: null
functional_acceptance: "../functional-acceptance.md"
updated_at: "2026-07-21"
---

# Iteration 002

## Current Understanding

Task agent 首轮验收认可 capability reports 和 gate 结论，但指出两个测试覆盖缺口：Windows timeout 子进程树清理没有单测覆盖，early-stop orphan daemon cleanup artifact 没有单测断言。这些属于 gate 工具可靠性问题，需修复后才能完成 goal。

## Implementation Approach

补充窄范围单测，不再改动已采集的真实 capability reports。测试通过 fake `Popen` / `subprocess.run` / `platform.system` 和 fake cleanup 函数覆盖系统副作用边界。

## Changes This Iteration

- 新增 `test_subprocess_runner_timeout_kills_windows_process_tree`，模拟 `Popen.communicate()` 超时并断言 Windows `taskkill /PID /T /F` 被调用。
- 新增 `test_core_command_early_stop_records_cleanup_evidence`，使用 `EarlyStopRunner` 触发真实 `SubprocessRunner` 类型分支，并断言 `early_stop.cleanup_evidence`、`artifact_index` 和 cleanup artifact 存在。
- 更新 iteration 001 的测试证据为 17 passed。

## Verification Evidence

- `python -m pytest -q "test/test_rmux_capability_probe.py"`：17 passed。
- `python -m py_compile "scripts/probe_rmux_capability.py"`：通过。
- `git diff --check`：通过。
- Task agent 复验 verdict：pass，见 `../functional-acceptance.md`。

## Problems Encountered

- capability gate 结果仍是不通过：`psmux 3.3.3` 存在 6 个 required blocking gaps，`rmux 0.9.0` 默认入口 `start-server` 超时。
- 这些 gap 不是本轮测试覆盖问题，而是后续 backend 基座或 capability 兼容问题。

## Next Attempt

本 goal 已完成。后续需要先处理 `psmux` required gaps 或更换 backend 基座，再创建新的 implementation goal。

## State Update

`state.yaml.status` 更新为 complete，`current_iteration` 更新为 2，并与 `../functional-acceptance.md` 双向引用。
