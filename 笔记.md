/goal .codestable/roadmap/windows-native-herdr-ccb
方向 A 作为后续增强：正常启动（有 job）时让 runtime_pid 反映真实进程，供 health/recovery 使用。
直接运行.\ccb8.cmd命令
优化采集脚本 watch 模式记录结果的方式，采用与全量采集的方式一样，每次采集的结果保存在新的类似watch-20260808-120250这样的目录中，取消-OutputDir参数，直接保存在.codestable\roadmap\windows-native-herdr-ccb\drafts\herdr-ui-integration-spike\watch中。

刚才运行了一次外部项目的采集脚本，发现采集脚本会进入循环状态，一直重复执行.\ccb8.cmd命令，直到用户按ctrl + c退出，此时weztrem启动了16个重复的herdr ui。不分析采集结果。先对采集脚本进行大改：
利用weztrem的多tab功能，用户先在第一个tab中启动采集脚本，此时的采集脚本不应该指定时长后自动关闭并清理ccb后台，也不应该主动执行.\ccb8.cmd命令（因为.\ccb8.cmd命令已经能正常启动ccb），而是要持续监控其他tab中，与CCB有关的全部信息并记录（要提供肉眼可视的动态变化的东西向用户展示脚本正在运行，避免用户误认脚本已经退出或者死掉），直到用户按ctrl + c退出完成采集。
增强采集脚本的功能，不但采集启动阶段时的信息，也要能监控启动后正常工作时的状态。
现在需要分析采集结果结合CCB自身日志分析herdr ui中的cli为何不能复用系统中已有配置项

现在的herdr ui中的codex和claude还是没有使用系统原有的配置项。
外部项目的ccb.config中，[ui.sidebar]这里要如何写才能完全禁用CCB自身的sidebar？在外部项目通过.\ccb8.cmd命令启动的herdr
  ui，会出现ccb的sidebar占位用的pane。
  另外要显式定义runtime.mux为herdr
  
2026-08-06-launch-mode-runtime-mux-backend任务已经进行到端对端验证，继续按既定顺序实施。实施前先提交git获得干净工作空间。实施过程中在关键节点使用open-code-review:review技能进行review且pass后才能继续。
已经在repo和ext分别执行了采集脚本。
& ".codestable/roadmap/windows-native-herdr-ccb/drafts/herdr-ui-integration-spike/run_spike.ps1" -ProjectRoot "E:\GitHub开源项目\TachiKuma\claude_code_bridge" -ObservedWindowsFlash -ObservedHerdrAgentsPanelText  "codex"
& "E:/GitHub开源项目/TachiKuma/claude_code_bridge/.codestable/roadmap/windows-native-herdr-ccb/drafts/herdr-ui-integration-spike/run_spike.ps1" ` -ProjectRoot "D:/C#Project/GitHub/AvaPrintDesigner" ` -Ccb8Path "D:/C#Project/GitHub/AvaPrintDesigner/ccb8.cmd" ` -RepoRoot "E:/GitHub开源项目/TachiKuma/claude_code_bridge" ` -ExpectedAgents 2 ` -ObservedWindowsFlash ` -ObservedHerdrAgentsPanelText "codex"

& "E:/GitHub开源项目/TachiKuma/claude_code_bridge/.codestable/roadmap/windows-native-herdr-ccb/drafts/herdr-ui-integration-spike/run_spike.ps1" `  -WatchMode `  -ProjectRoot "D:/C#Project/GitHub/AvaPrintDesigner" `  -RepoRoot "E:/GitHub开源项目/TachiKuma/claude_code_bridge" `  -WatchPollIntervalMs 2000 `  -WatchProcessInterval 5

你是 CodeStable 本次改动的独立代码审查 agent。只读，不修改文件。

  范围：当前仓库 `E:/GitHub开源项目/TachiKuma/claude_code_bridge` 中 `ccb8.ps1` 的本轮 wrapper-only `--log` 修复。注意工作区进入本轮前已有 `Run-BoundedKillForce` 和 `--wrapper-self-test exit 0` 的 dirty
  diff；本轮新增范围是 `Get-WrapperLogRoots`、`Show-WrapperLogs`、以及 `if ($CcbArgs[0] -ieq '--log')` 分支。

  用户问题：`./ccb8.cmd --log` 返回 `command_status: invalid` / `error: invalid start command`。修复意图：`--log` 在 PowerShell wrapper 层处理，初始化环境后列出最近日志根和日志尾部，不再落入 `ccb.py` start
  parser；不启动 source-dev CCB，不执行 prestart kill。

  请读取 `.codestable/attention.md`、当前 `git status --short`、`git diff -- ccb8.ps1`、`ccb8.ps1` 相关代码。按 blocking / important / nit / suggestion / residual-risk 输出发现；每条必须有 file:line 或仓库
  事实、影响和建议边界。额外给 Test And QA Focus。不要写 review 文件，只回传审查结果。
  
  
  
确认是“pane 已有但 provider 没启动”，还是“agent pane 创建后命令立即退出”。
需要处理几个事：
在外部项目中通过Herdr中的powershell直接运行.\ccb8.cmd命令，仍然没有出现ccb.config中定义的两个agent的Cli对话界面。只是看到了一堆cmd窗口闪现接着关闭了。在闪退前的一瞬间，我看到herdr的左边侧边栏中的agents面板中出现在了claude的字样。需要固化这个信息
如果是在Herdr中手动启动claude，则表现正常，但是肯定无法使用ccb的强大功能。
需要考虑CCB的sidebar在Herdr中的作用，几个方向：1.全功能的sidebar能否代替herdr的侧边栏；2.herdr的侧边栏代替ccb的sidebar
herdr能否接受CCB的pane布局配置（或者转换后的）
mux:w7V:p3中的w7V:p3可能不是固定字符串，这个需要进一步验证。
mux:w81:p4
agent_2 的 runtime_ref 不是 tmux:%...，而是 mux:w7V:p3，但 _binding_reject_reason() 只接受 tmux: 前缀，于是每次都把它当成不合格绑定，触发 relaunch_runtime:agent_2。这会直接制造你看到的 Python 拉起风暴。现在我找这个前缀的生成点和该函数的调用面。
仓库里至少有一批地方都把“有效 runtime”写死成 tmux:。
沿着绑定判定链读源码，重点找“把 mux: 当成无效 runtime”的位置。
看实际运行态证据，确认 mux:w7V:p3 只是前缀不兼容，还是连 pane 标识的形态也变了。这个会决定修复点是“放行前缀”还是“统一
  规范化”。
当前 runtime 已经是 mounted 且 state=running，所以“启动失败”不是现状了。更像是启动链路里还有个子进程被前台控制台打断后反复重试，或者某个窗口级 helper 在循环重生。我要看 startup-report 和最近的 lifecycle.jsonl 事件。

源码版CCB的启动过程会发生严重故障，直接死掉。请重点怀疑几个方向：Native Windows的简体中文环境时的默认GBK编解码、UTF-8有无BOM、源码版CCB自身项目的本地路径/外部项目的本地路径是否有中文或者特殊字符、ccb8.cmd和ccb8.ps1所在的路径(一般默认就是外部项目的本地路径)是否为写死的硬编码（可以考虑启动前先获取当前路径）
把D:\C#Project\GitHub\AvaPrintDesigner\ccb8.cmd拆分成ccb8.cmd和ccb8.ps1两个文件，仍然通过.\ccb8.cmd的方式来启动源码版CCB。
正常启动源码版CCB的过程中，可能会发生任何的意外导致CCB死掉，此时的Ctrl+C并不是owner/用户按下的，需要减少Ctrl+C对排障的干扰。
$env:CCB_NO_ATTACH="1"; $env:CCB_CCBD_FAULTHANDLER="1"; $env:PYTHONUNBUFFERED="1"; .\ccb8.cmd *>&1 | Tee-Object -FilePath ".\ccb8-start-$(Get-Date -Format yyyyMMdd-HHmmss).log"
.\ccb8.cmd --diagnose 确认原路径不坏
--log extra 确认新模式的参数保护能工作 
--log 日志模式
修改D:\C#Project\GitHub\AvaPrintDesigner\ccb8.cmd，要求启动ccb时先做 kill f动作，保证不影响已安装CCB（包括已经启动的v5版本ccb）的前提下清除任何的干扰项。
执行外部验证时，严禁在codex中直接启动CCB。

Herdr capability 选择/Native Windows fallback 逻辑，而不是 token 文件读取

外部项目验证时的 source/dev 隔离运行步骤：
假设源码 checkout 是：

  $ccbSrc = "E:/GitHub开源项目/TachiKuma/claude_code_bridge"
  $project = "D:/path/to/external-project"

  先在当前 PowerShell 会话里建临时 shim：

  $shim = Join-Path $env:TEMP "ccb-herdr-source-shim"
  New-Item -ItemType Directory -Force -Path $shim | Out-Null

  @"
  @echo off
  set "PYTHONPATH=$ccbSrc\lib;%PYTHONPATH%"
  python "$ccbSrc\ccb.py" %*
  "@ | Set-Content -Encoding ASCII -LiteralPath (Join-Path $shim "ccb.cmd")

  $env:PATH = "$shim;$env:PATH"
  $env:PYTHONPATH = "$ccbSrc\lib;$env:PYTHONPATH"
  $env:CCB_SOURCE_ALLOWED_ROOTS = $project
  $env:CCB_HERDR_EXE = "C:/Users/Administrator/AppData/Local/Programs/Herdr/herdr.exe"
  $env:CCB_HERDR_SESSION = "ccb-herdr-external-smoke"
  $env:CCB_HERDR_CAPABILITY_REPORT = "$ccbSrc/.codestable/features/2026-07-31-herdr-backend-contract-spike/evidence/herdr-contract-spike-
  evidence.json"

  然后在外部项目运行：

  cd $project
  ccb --print-version
  ccb doctor --output
  ccb -n

  验证确实走的是源码版而不是已安装版：

  where.exe ccb
  ccb doctor --output | Select-String "install_path|windows_x64_release_surface|ccbd_herdr"

  停止这次隔离运行：

  cd $project
  ccb kill

  恢复当前 shell，不影响已安装版本：

  Remove-Item Env:CCB_SOURCE_ALLOWED_ROOTS -ErrorAction SilentlyContinue
  Remove-Item Env:CCB_HERDR_EXE -ErrorAction SilentlyContinue
  Remove-Item Env:CCB_HERDR_SESSION -ErrorAction SilentlyContinue
  Remove-Item Env:CCB_HERDR_CAPABILITY_REPORT -ErrorAction SilentlyContinue

  注意两点：

  1. 这是源码/dev 验证路径，不是 npm release supported 路径；Windows npm release route 当前仍是 diagnostic-only。
  2. CCB_HERDR_CAPABILITY_REPORT 目前使用 roadmap 中已验收的 Herdr capability evidence。若 Herdr 版本或本机状态变化较大，应该重新捕获
     capability evidence 后再跑 full workflow。
/goal "执行 CodeStable roadmap 目录 .codestable/roadmap/windows-native-herdr-ccb 下的 goal 执行包。先读取 goal-protocol.md、goal-protocol-feature-loop.md、goal-protocol-gates.md、goal-protocol-audit.md、goal-state.yaml、goal-plan.md；这是已由用户确认 roadmap 和全部 feature design，并在同一次 Goal 启动确认中授权 Goal acceptance 与每个 feature 自动 scoped-commit 的模式，两项 ApprovalRef 仍须分别机械核验。按 goal-state.yaml 的 features 顺序循环：进入 cs-feat implementation、cs-code-review、cs-feat QA；review/QA 失败按协议修复重跑，awaiting/needs-human/blocked 分别等待、请求输入或 handoff。QA passed 后只用 goal-acceptance ApprovalRef 调用 ResumeGoalAcceptance；accept 后先持久化 accepted 状态与新 index，再机械核验 goal-commits ApprovalRef，只有有效时才 scoped-commit 本 feature 的全部状态更新，缺失、不匹配或 rejected必须 handoff 且不得提交。每个 feature 完成打印 CS_ROADMAP_GOAL_FEATURE_DONE；全部完成后做最终 roadmap 审计。只有出现 CS_ROADMAP_GOAL_COMPLETE，且所有 feature review/QA/acceptance、授权提交和最终审计均通过、没有 CS_ROADMAP_GOAL_HANDOFF，本 goal 才算完成。"

历史 child 缺少若干 gate JSON

某些节点必须要在真正改代码前进行git commit的提交，以符合OCR（Open Code Review）的使用要求。

宽回归暴露一个既有 Windows 路径分隔断言：期望 /home/...，实际由 Path 转成 \home\...

广义筛选里出现多项 Windows 基线失败：ping 路径分隔符、doctor tmux command quoting、diagnostics bundle 在 Windows 临时深路径下路径过长、layout dynamic-smoke 依赖 tmux 环境。

“我没有运行 pytest；用户要求只读，pytest 可能写 .pytest_cache / pycache。”所谓的只读只限于代码、.codestable文档、.codegraph索引、程序用到的资源文件等、git相关等。需要对subagent的读写权限进行更精细的控制。

当前实现重点是把真实 Herdr workspace/pane 映射为 ccbd 所需的 logical window/root-pane facade，并保持 tmux-only UI 路径关闭。先探查 Herdr 的 metadata 命令，以便让 logical window 在同一 workspace 中可稳定恢复，而不是只靠进程内缓存。

结论：我没有发现会阻塞合并的实质问题。当前实现满足这次 feature 的核心边界：Herdr 走 herdr-native，没有伪装成 tmux-family；Windows auto/default 的 fail-closed 路径、blocked fixture、scope
  guard 和回归测试都对上了设计。

  blocking

  - none

  important

  - none

  nit

  - none

  suggestion

  - none

  praise

  - lib/terminal_runtime/mux_backend_contract.py、lib/terminal_runtime/backend_resolver.py、lib/terminal_runtime/fake_mux_backend.py 的拆分很干净，契约、回退和 fake fixture 没互相污染。
  - test/test_mux_backend_contract.py:36-180 和 test/test_terminal_runtime_backend_selection.py:88-273 对 fail-closed、blocked report、Windows gate、缺证据场景覆盖得比较直接。

  residual-risk

  - 现在的 scope 保护主要靠字符串和路径守卫，能挡住这次显式范围，但对“没出现 herdr-native 字面量”的隐蔽生产接线不算结构性防护。相关位置是 [test/test_herdr_spike_no_production_route.py](D:/
    Python/GitHub/claude_code_bridge/test/test_herdr_spike_no_production_route.py:26) 和 [.codestable/features/2026-07-31-mux-backend-contract-herdr-v2/mux-backend-contract-herdr-v2-
    checklist.yaml](D:/Python/GitHub/claude_code_bridge/.codestable/features/2026-07-31-mux-backend-contract-herdr-v2/mux-backend-contract-herdr-v2-checklist.yaml:105)。

  - FakeMuxBackend 的默认 backend_impl 现在是 herdr，见 [lib/terminal_runtime/fake_mux_backend.py](D:/Python/GitHub/claude_code_bridge/lib/terminal_runtime/fake_mux_backend.py:29)；仓库里当
    前没有别的无参调用点，但这是一个可见的 API 默认值变化。

  Test And QA Focus

  - 继续盯住 Windows x64 + auto + 缺 capability evidence、malformed blocked report、unsupported/unknown gap 这三类反例。
  - 再跑一次生产入口扫描，确认 api.py / api_selection.py 没有偷偷接入 resolve_mux_backend_v2。
  - 如果后续 spike evidence 出现 continue-with-gaps，要补一条针对 partial/workaround 的回归，确认现在的 fail-closed 语义是否仍符合后续约定。