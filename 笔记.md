& "./ccb-src.ps1" kill -f; if ($LASTEXITCODE -eq 0) { & "./ccb-src.ps1" } else { exit $LASTEXITCODE }

$src="E:/GitHub开源项目/TachiKuma/claude_code_bridge";  $ccbSrc="D:/C#Project/GitHub/CodeStable/.codestable/tools/ccb-src.ps1"; if (-not (Get-Command cargo  -ErrorAction SilentlyContinue)) { $rustup=Join-Path $env:TEMP "rustup-init.exe"; Invoke-WebRequest  "https://win.rustup.rs/x86_64" -OutFile $rustup; & $rustup -y --profile minimal --default-toolchain  stable --no-modify-path; $env:Path="$env:USERPROFILE\.cargo\bin;$env:Path" };  $vswhere="${env:ProgramFiles(x86)}/Microsoft Visual Studio/Installer/vswhere.exe"; if (Test-Path  $vswhere) { $dev=& $vswhere -latest -products * -requires  Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -find "Common7\Tools\Launch-VsDevShell.ps1" |  Select-Object -First 1; if ($dev) { & $dev -SkipAutomaticLocation } }; cargo build --release  --manifest-path "$src/tools/ccb-agent-sidebar/Cargo.toml"; if ($LASTEXITCODE -ne 0) { exit  $LASTEXITCODE }; & $ccbSrc kill -f; if ($LASTEXITCODE -eq 0) { & $ccbSrc } else { exit $LASTEXITCODE }

测试中发现，非sidebar类型的pane会在鼠标滚轮动作时，会在pane的右上角动态显示黄底黑子的\[0\/0\]，但只有claude所在的pane，能反映鼠标的滚动，能向上翻看chat的聊天记录。在滚轮转动时， [0\/0\]里的数字会变化，同时聊天内容同步滚动。另外三个codex的pane仍然无法滚动查看历史记录。

WezTerm配置文件中的选择复制右键粘贴，在pane里面的cli中的输入框失效了，右键粘贴的是非预期内容。
  pane的右
  
 1. 普通 pane 单击聚焦 pass。
  2. 普通 pane 拖拽选区 fail，可以选中文本，但是在main_coder和archi两个pane，选区的起点会在鼠标的下一行（左右的起点正确），非预期，code_reviewer和ccb_self两个pane的拖拽选区正常；4个普通pane拖拽时会触发 pane 单击聚焦，预期现象。如果ccb.config设置成main = "Main_Code:codex; code_reviewer:codex"，只启用2个pane，则拖拽选区 fail，选区故障依旧。
  3. 普通 pane 右键行为 fail，没有在cli中粘贴，与WezTerm的配置文件（C:\Users\Administrator\.wezterm.lua）中的左键拖拽复制右键粘贴设置不符
  4. 普通 pane 滚轮行为WezTerm  fail，没有观察到任何的滚动行为
  5. 侧栏 settings 点击  fail，没有反应
  6. 侧栏 x KillProject 点击  fail，没有反应 
  
 通过github Desktop从远端拉取了两个 commit，本地的更改由github Desktop设置成暂存的更改。现在需要保持行为/逻辑不变从代码层面进行合并。
 
 1. 普通 pane 单击聚焦 pass。
  2. 普通 pane 拖拽选区 fail，可以选中文本，但是在main_coder和archi两个pane，选区的起点会在鼠标的下一行（左右的起点正确），非预期，code_reviewer和ccb_self两个pane的拖拽选区正常；4个普通pane拖拽时会触发 pane 单击聚焦，预期现象。如果ccb.config设置成main = "Main_Code:codex; code_reviewer:codex"，只启用2个pane，则拖拽选区 fail，选区故障依旧。也就是2x2的pane时，处于下部的pane，拖拽选区正常，处于上部的pane，拖拽选区不正常。1x1左右pane布局时，两个pane的拖拽选区不正常。1x1上下pane布局时，只有处于下方的pane的拖拽选区正常。
  3. 普通 pane 右键行为 fail：已经实现右键粘贴，但是当选区多行时，会发生cli识别到换行的回车，从而粘贴的字符串被分割成多个部分并被cli执行，与预期的只粘贴不发送不符，发送由用户手动按回车确认发送信息。
  4. 普通 pane 滚轮行为WezTerm  fail，没有观察到任何的滚动行为
  5. 侧栏 settings 点击  fail，没有反应
  6. 侧栏 x KillProject 点击  fail，没有反应  
 